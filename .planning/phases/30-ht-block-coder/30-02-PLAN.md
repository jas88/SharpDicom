---
phase: 30-ht-block-coder
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/SharpDicom/Codecs/Jpeg2000/Tier1/IBlockCoder.cs
  - src/SharpDicom/Codecs/Jpeg2000/Tier1/EbcotBlockCoder.cs
  - src/SharpDicom/Codecs/Jpeg2000/J2kEncoder.cs
  - src/SharpDicom/Codecs/Jpeg2000/J2kDecoder.cs
  - tests/SharpDicom.Tests/Codecs/Jpeg2000/Tier1/EbcotBlockCoderTests.cs
autonomous: true

must_haves:
  truths:
    - "IBlockCoder provides unified interface for EBCOT and future HT block coding"
    - "J2kEncoder uses SubbandPartitioner to assign correct subband types to code-blocks"
    - "J2kDecoder uses SubbandPartitioner to decode with correct subband context"
    - "Existing EBCOT tests still pass after refactor"
  artifacts:
    - path: "src/SharpDicom/Codecs/Jpeg2000/Tier1/IBlockCoder.cs"
      provides: "Shared block coder interface"
      contains: "interface IBlockCoder"
    - path: "src/SharpDicom/Codecs/Jpeg2000/Tier1/EbcotBlockCoder.cs"
      provides: "EBCOT wrapped behind IBlockCoder"
      contains: "class EbcotBlockCoder"
    - path: "src/SharpDicom/Codecs/Jpeg2000/J2kEncoder.cs"
      provides: "Fixed encoder using subband partitioner"
      contains: "SubbandPartitioner"
  key_links:
    - from: "J2kEncoder"
      to: "SubbandPartitioner"
      via: "Code-block subband type lookup"
      pattern: "SubbandPartitioner\\."
    - from: "J2kEncoder"
      to: "IBlockCoder"
      via: "Block coder abstraction for encode"
      pattern: "IBlockCoder"
    - from: "EbcotBlockCoder"
      to: "EbcotEncoder"
      via: "Delegates to existing EBCOT implementation"
      pattern: "EbcotEncoder"
---

<objective>
Extract EBCOT behind an IBlockCoder interface and fix the J2K pipeline to use proper multi-resolution subband types instead of hardcoded LL.

Purpose: This creates the abstraction layer that allows the HT block coder to slot in alongside EBCOT, and fixes the critical LL-only subband bug (J2kEncoder.cs line 332) that produces incorrect context information for all non-LL code-blocks.

Output: IBlockCoder interface, EbcotBlockCoder wrapper, and J2kEncoder/J2kDecoder updated to use SubbandPartitioner from Plan 01.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/SharpDicom/Codecs/Jpeg2000/Tier1/EbcotEncoder.cs
@src/SharpDicom/Codecs/Jpeg2000/Tier1/EbcotDecoder.cs
@src/SharpDicom/Codecs/Jpeg2000/J2kEncoder.cs
@src/SharpDicom/Codecs/Jpeg2000/J2kDecoder.cs
@.planning/phases/30-ht-block-coder/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IBlockCoder interface and EbcotBlockCoder wrapper</name>
  <files>
    src/SharpDicom/Codecs/Jpeg2000/Tier1/IBlockCoder.cs
    src/SharpDicom/Codecs/Jpeg2000/Tier1/EbcotBlockCoder.cs
  </files>
  <action>
Create IBlockCoder interface:
```csharp
public interface IBlockCoder
{
    CodeBlockData EncodeBlock(
        ReadOnlySpan<int> coefficients,
        int width, int height,
        int subbandType,
        int msbPosition);

    void DecodeBlock(
        ReadOnlySpan<byte> data,
        int numPasses,
        Span<int> output,
        int width, int height,
        int msbPosition,
        int subbandType);
}
```

Create EbcotBlockCoder that wraps existing EbcotEncoder/EbcotDecoder behind IBlockCoder:
- Singleton pattern (Instance static property) since EBCOT encoder/decoder are already effectively stateless at the block level
- EncodeBlock delegates to EbcotEncoder.EncodeCodeBlock
- DecodeBlock delegates to EbcotDecoder.DecodeCodeBlock
- Handle the array/span conversion needed between existing API and new interface

Do NOT modify EbcotEncoder.cs or EbcotDecoder.cs internals -- wrap them.
  </action>
  <verify>
`dotnet build /Users/jas88/Developer/Github/SharpDicom/src/SharpDicom/SharpDicom.csproj` succeeds.
  </verify>
  <done>
IBlockCoder interface exists. EbcotBlockCoder wraps existing EBCOT implementation without modifying it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix J2kEncoder and J2kDecoder subband routing</name>
  <files>
    src/SharpDicom/Codecs/Jpeg2000/J2kEncoder.cs
    src/SharpDicom/Codecs/Jpeg2000/J2kDecoder.cs
    tests/SharpDicom.Tests/Codecs/Jpeg2000/Tier1/EbcotBlockCoderTests.cs
  </files>
  <action>
Update J2kEncoder.EncodeComponentCodeBlocks:
- Replace the hardcoded `int subbandType = 0;` (line 332) with proper subband lookup
- Use SubbandPartitioner.GetSubbands() to build the subband descriptors for the image
- For each code-block, determine which subband it belongs to based on its position in the DWT coefficient array and the decomposition level
- Use TileComponent to manage coefficient extraction per subband
- Accept IBlockCoder parameter (default to EbcotBlockCoder.Instance) to enable future HT routing
- Keep backward compatibility: existing J2kEncoderOptions still work identically

Update J2kDecoder.DecodeFrame:
- Similarly replace hardcoded subbandType=0 with proper subband lookup
- Use SubbandPartitioner for code-block to subband mapping during decode
- Accept IBlockCoder parameter (default to EbcotBlockCoder.Instance)

Create EbcotBlockCoderTests:
- Test that EbcotBlockCoder.Instance.EncodeBlock produces same output as direct EbcotEncoder.EncodeCodeBlock call
- Test that DecodeBlock produces same output as direct EbcotDecoder.DecodeCodeBlock call
- Roundtrip test: encode with EbcotBlockCoder, decode with EbcotBlockCoder, verify coefficients match
  </action>
  <verify>
`dotnet test /Users/jas88/Developer/Github/SharpDicom/tests/SharpDicom.Tests/ --filter "FullyQualifiedName~Jpeg2000"` passes all J2K-related tests. `dotnet test /Users/jas88/Developer/Github/SharpDicom/tests/SharpDicom.Tests/` passes all tests (no regressions).
  </verify>
  <done>
J2kEncoder and J2kDecoder use correct subband types from SubbandPartitioner. EbcotBlockCoder passes roundtrip tests. No regression in existing test suite.
  </done>
</task>

</tasks>

<verification>
- `dotnet build /Users/jas88/Developer/Github/SharpDicom/SharpDicom.sln` succeeds
- `dotnet test /Users/jas88/Developer/Github/SharpDicom/tests/SharpDicom.Tests/` passes (2456+ pass, 0 fail)
- The comment "For simplicity, use LL subband type (0)" is removed from J2kEncoder.cs
</verification>

<success_criteria>
- IBlockCoder interface provides unified block coding abstraction
- EbcotBlockCoder wraps existing EBCOT without modifying its internals
- J2kEncoder uses SubbandPartitioner for correct subband type assignment
- J2kDecoder uses SubbandPartitioner for correct subband type during decode
- All existing tests pass (no regressions from subband fix)
</success_criteria>

<output>
After completion, create `.planning/phases/30-ht-block-coder/30-02-SUMMARY.md`
</output>
