---
phase: 29-mongodb-bson-serialization
plan: 04
type: execute
wave: 3
depends_on: [29-02, 29-03]
files_modified:
  - tests/SharpDicom.Tests/Serialization/BsonDocumentBufferTests.cs
  - tests/SharpDicom.Tests/Serialization/BsonDicomWriterTests.cs
  - tests/SharpDicom.Tests/Serialization/BsonDicomReaderTests.cs
  - tests/SharpDicom.Tests/Serialization/BsonRoundtripTests.cs
  - tests/SharpDicom.Tests/Serialization/DicomJsonWriterTests.cs
  - tests/SharpDicom.Tests/Serialization/DicomJsonReaderTests.cs
  - tests/SharpDicom.Tests/Serialization/DicomJsonRoundtripTests.cs
autonomous: true

must_haves:
  truths:
    - "BSON roundtrip preserves all standard VR types"
    - "BSON roundtrip preserves private tags with creator names"
    - "BSON roundtrip preserves nested sequences"
    - "Dual-storage VRs (IS/DS/DA/TM/DT) roundtrip with exact original string values"
    - "PN roundtrip preserves all three component groups"
    - "DICOM-JSON roundtrip preserves all VR types"
    - "Binary data below threshold inlines; above threshold triggers handler"
    - "BsonDocumentBuffer correctly handles deferred-size documents"
  artifacts:
    - path: "tests/SharpDicom.Tests/Serialization/BsonRoundtripTests.cs"
      provides: "End-to-end BSON serialization roundtrip verification"
    - path: "tests/SharpDicom.Tests/Serialization/DicomJsonRoundtripTests.cs"
      provides: "End-to-end DICOM-JSON roundtrip verification"
    - path: "tests/SharpDicom.Tests/Serialization/BsonDocumentBufferTests.cs"
      provides: "Low-level buffer correctness tests"
  key_links:
    - from: "BsonRoundtripTests"
      to: "BsonDicomWriter/BsonDicomReader"
      via: "Serialize then Deserialize, compare datasets"
    - from: "DicomJsonRoundtripTests"
      to: "DicomJsonWriter/DicomJsonReader"
      via: "Serialize then Deserialize, compare datasets"
---

<objective>
Comprehensive test suite for BSON and DICOM-JSON serialization, covering all VR types, edge cases, and roundtrip fidelity.

Purpose: Verify that the serialization layer maintains data integrity for all DICOM element types and configuration options.
Output: 7 test files with 60+ tests covering both BSON and DICOM-JSON paths.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-mongodb-bson-serialization/29-CONTEXT.md
@.planning/phases/29-mongodb-bson-serialization/29-01-SUMMARY.md
@.planning/phases/29-mongodb-bson-serialization/29-02-SUMMARY.md
@.planning/phases/29-mongodb-bson-serialization/29-03-SUMMARY.md
@src/SharpDicom/Serialization/Bson/BsonDicomWriter.cs
@src/SharpDicom/Serialization/Bson/BsonDicomReader.cs
@src/SharpDicom/Serialization/Bson/DicomJsonWriter.cs
@src/SharpDicom/Serialization/Bson/DicomJsonReader.cs
@src/SharpDicom/Serialization/Bson/BsonDocumentBuffer.cs
@src/SharpDicom/Serialization/Bson/BsonSerializationOptions.cs
@tests/SharpDicom.Tests/SharpDicom.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: BsonDocumentBuffer tests and BSON serialization/deserialization/roundtrip tests</name>
  <files>
    tests/SharpDicom.Tests/Serialization/BsonDocumentBufferTests.cs
    tests/SharpDicom.Tests/Serialization/BsonDicomWriterTests.cs
    tests/SharpDicom.Tests/Serialization/BsonDicomReaderTests.cs
    tests/SharpDicom.Tests/Serialization/BsonRoundtripTests.cs
  </files>
  <action>
    Create `tests/SharpDicom.Tests/Serialization/` directory and implement:

    **BsonDocumentBufferTests.cs** (~10 tests):
    - `WriteInt32_WritesLittleEndian` — verify byte order
    - `WriteInt64_WritesLittleEndian` — verify byte order
    - `WriteDouble_WritesCorrectBits` — verify via BitConverter roundtrip
    - `WriteCString_NullTerminated` — verify UTF-8 + 0x00
    - `WriteBsonString_LengthPrefixedNullTerminated` — verify int32 length + UTF-8 + 0x00
    - `BeginEndDocument_PatchesSize` — verify deferred size patching is correct
    - `NestedDocuments_CorrectSizes` — nested BeginDocument/EndDocument
    - `EnsureCapacity_GrowsBuffer` — write enough to trigger resize
    - `Reset_ClearsPosition` — verify Position returns to 0
    - `ToArray_CopiesWrittenBytes` — verify correct slice

    **BsonDicomWriterTests.cs** (~15 tests):
    - `Serialize_EmptyDataset_ReturnsMinimalBsonDocument` — 5 bytes (size + terminator)
    - `Serialize_StringElement_WritesValueArray` — verify LO/SH produce {"Value": ["str"]}
    - `Serialize_IntegerString_DualStorage` — IS element produces {"Value": [42], "Raw": "42"}
    - `Serialize_DecimalString_DualStorage` — DS element produces {"Value": [3.14], "Raw": "3.14"}
    - `Serialize_Date_DualStorage` — DA element produces {"Value": [datetime], "Raw": "20240115"}
    - `Serialize_Time_DualStorage` — TM element produces {"Value": [ms_from_midnight], "Raw": "120000"}
    - `Serialize_PersonName_WithComponents` — PN with Alphabetic/Ideographic groups
    - `Serialize_NumericElements_CorrectBsonTypes` — SS=Int32, UL=Int64, FD=Double
    - `Serialize_Sequence_NestedDocuments` — SQ produces array of documents
    - `Serialize_BinaryElement_InlinesBelowThreshold` — small OB is BsonBinaryData
    - `Serialize_BinaryElement_ExternalAboveThreshold` — large OB with handler produces reference
    - `Serialize_PrivateTags_GroupedByCreator` — _private sub-document structure
    - `Serialize_StripPrivateTags_OmitsPrivate` — StripPrivateTags=true
    - `Serialize_TagKeyFormat_Hex8` — default format
    - `Serialize_TagKeyFormat_Keyword` — keyword format uses dictionary names
    - `Serialize_VrField_OnlyWhenAmbiguous` — default mode excludes vr for standard tags

    **BsonDicomReaderTests.cs** (~10 tests):
    - `Deserialize_MinimalDocument_ReturnsEmptyDataset`
    - `Deserialize_StringElement_RestoresValue`
    - `Deserialize_DualStorageIS_RestoresFromRaw` — IS "Raw" field used for reconstruction
    - `Deserialize_PersonName_RestoresOriginalString`
    - `Deserialize_NumericElement_RestoresValue` — binary reconstruction correct
    - `Deserialize_Sequence_RestoresNestedDatasets`
    - `Deserialize_BinaryElement_RestoresBytes`
    - `Deserialize_PrivateTags_RestoresWithCreator`
    - `Deserialize_AllTagKeyFormats` — hex, dotted, keyword all parsed correctly
    - `Deserialize_MissingVr_LooksUpInDictionary`

    **BsonRoundtripTests.cs** (~15 tests):
    - `Roundtrip_AllStringVRs` — AE, AS, CS, DA, DS, DT, IS, LO, LT, PN, SH, ST, TM, UC, UI, UR, UT
    - `Roundtrip_AllNumericVRs` — SS, US, SL, UL, FL, FD, SV, UV, AT
    - `Roundtrip_BinaryVRs` — OB, OW, OD, OF, OL
    - `Roundtrip_Sequence_WithNestedElements`
    - `Roundtrip_SequenceDepth_ExceedsLimit_FallsBackToBinary`
    - `Roundtrip_PrivateTags_PreservesCreatorNames`
    - `Roundtrip_EmptyElements`
    - `Roundtrip_MultiValuedElements` — VM>1 with backslash-delimited values
    - `Roundtrip_IS_PreservesExactString` — "042" roundtrips without becoming "42"
    - `Roundtrip_DS_PreservesExactString` — "3.14000" roundtrips without trimming
    - `Roundtrip_DA_PreservesExactString` — "20240115" survives DateTime conversion
    - `Roundtrip_TM_PreservesExactString` — "120000.000000" roundtrips
    - `Roundtrip_PN_PreservesComponentGroups` — Alphabetic=Ideographic=Phonetic
    - `Roundtrip_LargeDataset_AllTypesPresent` — comprehensive dataset with 20+ elements
    - `Roundtrip_IBufferWriter_MatchesByteArrayOutput` — both Serialize overloads produce identical bytes

    For roundtrip comparison, build a `DicomDataset` programmatically, serialize to BSON, deserialize back, and compare:
    - Same number of elements
    - Same tags present
    - Same VRs
    - Same string values (for string VRs: GetString comparison)
    - Same binary values (for numeric/binary VRs: RawValue.Span.SequenceEqual)

    Use NUnit `[TestFixture]` and `[Test]` attributes. Use `Assert.That()` style assertions.
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Tests/ --filter "FullyQualifiedName~Serialization.Bson"` — all tests pass.
  </verify>
  <done>
    ~50 BSON tests pass covering all VR types, dual storage, private tags, sequences, edge cases, and full roundtrip fidelity.
  </done>
</task>

<task type="auto">
  <name>Task 2: DICOM-JSON serialization/deserialization tests</name>
  <files>
    tests/SharpDicom.Tests/Serialization/DicomJsonWriterTests.cs
    tests/SharpDicom.Tests/Serialization/DicomJsonReaderTests.cs
    tests/SharpDicom.Tests/Serialization/DicomJsonRoundtripTests.cs
  </files>
  <action>
    **DicomJsonWriterTests.cs** (~8 tests):
    - `Serialize_EmptyDataset_ReturnsEmptyJsonObject` — "{}"
    - `Serialize_StringElement_VrAlwaysPresent` — {"00100020": {"vr": "LO", "Value": ["PAT001"]}}
    - `Serialize_PersonName_AlphabeticObject` — {"00100010": {"vr": "PN", "Value": [{"Alphabetic": "Doe^John"}]}}
    - `Serialize_IntegerString_AsJsonNumber` — IS Value is JSON number, not string
    - `Serialize_Sequence_NestedJsonObjects`
    - `Serialize_BinaryElement_InlineBinaryBase64` — OB produces "InlineBinary" field with base64
    - `Serialize_BinaryElement_BulkDataURI` — large OB with handler produces "BulkDataURI"
    - `Serialize_EmptyElement_NoValueField` — empty element has only {"vr": "XX"}

    **DicomJsonReaderTests.cs** (~6 tests):
    - `Deserialize_EmptyJsonObject_ReturnsEmptyDataset`
    - `Deserialize_StringElement_RestoresValue`
    - `Deserialize_PersonName_RestoresFromAlphabetic`
    - `Deserialize_InlineBinary_DecodesBase64`
    - `Deserialize_Sequence_RestoresNestedDatasets`
    - `Deserialize_UnknownVr_FallsBackToUN`

    **DicomJsonRoundtripTests.cs** (~8 tests):
    - `Roundtrip_AllStringVRs`
    - `Roundtrip_AllNumericVRs`
    - `Roundtrip_BinaryVRs`
    - `Roundtrip_Sequence`
    - `Roundtrip_PersonName`
    - `Roundtrip_MultiValuedElements`
    - `Roundtrip_EmptyElements`
    - `Roundtrip_LargeDataset_AllTypesPresent`

    Comparison logic: same as BSON roundtrip — build dataset, serialize to JSON, deserialize, compare element-by-element.

    For DICOM-JSON tests, additionally verify the JSON structure by parsing with JsonDocument and checking:
    - Root is a JSON object
    - Each key is 8-char hex
    - Each value has a "vr" field
    - "Value" is always an array when present

    Use NUnit `[TestFixture]` and `[Test]` attributes.
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Tests/ --filter "FullyQualifiedName~Serialization.DicomJson"` — all tests pass.
  </verify>
  <done>
    ~22 DICOM-JSON tests pass covering serialization, deserialization, and roundtrip for all VR types and edge cases.
  </done>
</task>

</tasks>

<verification>
- `dotnet test` passes (all new tests + no regressions in existing 4984 tests)
- Total new test count: ~70 tests
- Zero test failures
</verification>

<success_criteria>
- ~70 new tests in tests/SharpDicom.Tests/Serialization/
- All BSON roundtrip tests pass for every VR type
- All DICOM-JSON roundtrip tests pass
- Dual-storage VRs preserve exact original string values
- Private tags roundtrip with correct creator registration
- BsonDocumentBuffer internal behavior verified
</success_criteria>

<output>
After completion, create `.planning/phases/29-mongodb-bson-serialization/29-04-SUMMARY.md`
</output>
