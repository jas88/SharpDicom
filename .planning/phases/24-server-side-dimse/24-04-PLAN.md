---
phase: 24-server-side-dimse
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03"]
files_modified:
  - tests/SharpDicom.Tests/Network/ScpIntegrationTests.cs
  - tests/SharpDicom.Tests/Storage/FileSystemDicomStoreTests.cs
  - tests/SharpDicom.Tests/Network/DicomQueryMatcherTests.cs
autonomous: true

must_haves:
  truths:
    - "C-FIND SCP roundtrip: client sends C-FIND query, server returns matching results via Pending responses"
    - "C-STORE + C-FIND roundtrip: store files via C-STORE, then query and find them via C-FIND"
    - "FileSystemDicomStore stores files and indexes metadata that is queryable"
    - "DicomQueryMatcher wildcard translation is correct for all cases"
    - "DicomDateRange parses all DICOM date range formats correctly"
    - "Return key filtering only includes requested tags"
  artifacts:
    - path: "tests/SharpDicom.Tests/Network/ScpIntegrationTests.cs"
      provides: "End-to-end C-FIND/C-MOVE/C-GET roundtrip tests"
      contains: "CFindScp_ReturnsMatchingResults"
    - path: "tests/SharpDicom.Tests/Storage/FileSystemDicomStoreTests.cs"
      provides: "FileSystemDicomStore storage and retrieval tests"
      contains: "StoreAndRetrieve_RoundTrip"
    - path: "tests/SharpDicom.Tests/Network/DicomQueryMatcherTests.cs"
      provides: "Unit tests for wildcard matching, date range parsing, return key filtering"
      contains: "DicomWildcardToSqlLike"
  key_links:
    - from: "tests/SharpDicom.Tests/Network/ScpIntegrationTests.cs"
      to: "src/SharpDicom/Network/DicomServer.cs"
      via: "Creates DicomServer with SCP callbacks, connects DicomClient, performs Q/R operations"
      pattern: "DicomServer|DicomClient|CFindScu"
    - from: "tests/SharpDicom.Tests/Storage/FileSystemDicomStoreTests.cs"
      to: "src/SharpDicom/Storage/FileSystemDicomStore.cs"
      via: "Creates FileSystemDicomStore, stores files, queries index"
      pattern: "FileSystemDicomStore"
---

<objective>
Create comprehensive tests for all Phase 24 functionality: unit tests for query matching, integration tests for SCP roundtrips, and FileSystemDicomStore end-to-end tests.

Purpose: Verify that all SCP services work correctly in end-to-end scenarios, ensuring DICOM conformance and correct behavior. These tests also serve as documentation for how to use the SCP functionality.

Output: ~30-40 NUnit tests covering DicomQueryMatcher, DicomDateRange, C-FIND SCP roundtrip, C-STORE+C-FIND roundtrip, FileSystemDicomStore storage and retrieval.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-server-side-dimse/24-CONTEXT.md
@.planning/phases/24-server-side-dimse/24-01-SUMMARY.md
@.planning/phases/24-server-side-dimse/24-02-SUMMARY.md
@.planning/phases/24-server-side-dimse/24-03-SUMMARY.md
@tests/SharpDicom.Tests/Network/CEchoIntegrationTests.cs
@tests/SharpDicom.Tests/Network/DicomServerTests.cs
@src/SharpDicom/Network/DicomServer.cs
@src/SharpDicom/Network/DicomServerOptions.cs
@src/SharpDicom/Storage/FileSystemDicomStore.cs
@src/SharpDicom/Network/Dimse/Services/DicomQueryMatcher.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for DicomQueryMatcher and DicomDateRange</name>
  <files>
    tests/SharpDicom.Tests/Network/DicomQueryMatcherTests.cs
  </files>
  <action>
  Create `tests/SharpDicom.Tests/Network/DicomQueryMatcherTests.cs` with NUnit tests:

  **DicomWildcardToSqlLike tests** (~10 tests):
  - `WildcardToSql_AsteriskBecomesPercent` — "Smith*" -> "Smith%"
  - `WildcardToSql_QuestionMarkBecomesUnderscore` — "Sm?th" -> "Sm_th"
  - `WildcardToSql_LiteralPercentEscaped` — "100%" -> "100\%"
  - `WildcardToSql_LiteralUnderscoreEscaped` — "test_value" -> "test\_value"
  - `WildcardToSql_BackslashEscaped` — "a\\b" -> "a\\\\b"
  - `WildcardToSql_ComplexPattern` — "Sm*th?" -> "Sm%th_"
  - `WildcardToSql_NoWildcards` — "Smith" -> "Smith" (and hasWildcard=false)
  - `WildcardToSql_AllWildcard` — "*" -> "%"
  - `HasDicomWildcard_WithAsterisk_ReturnsTrue`
  - `HasDicomWildcard_WithoutWildcard_ReturnsFalse`

  **DicomDateRange tests** (~8 tests):
  - `Parse_SingleDate_ReturnsSameFromTo` — "20240115" -> (2024-01-15, 2024-01-15)
  - `Parse_FullRange_ReturnsFromTo` — "20240101-20240131" -> (Jan 1, Jan 31)
  - `Parse_OpenStart_ReturnsNullFrom` — "-20240131" -> (null, Jan 31)
  - `Parse_OpenEnd_ReturnsNullTo` — "20240101-" -> (Jan 1, null)
  - `Parse_Empty_ReturnsNulls` — "" -> (null, null)
  - `Parse_Null_ReturnsNulls` — null -> (null, null)
  - `Contains_DateInRange_ReturnsTrue`
  - `Contains_DateOutOfRange_ReturnsFalse`

  **FilterReturnKeys tests** (~5 tests):
  - `FilterReturnKeys_OnlyIncludesRequestedTags` — match has 10 tags, request has 3, result has 3
  - `FilterReturnKeys_AlwaysIncludesQueryRetrieveLevel` — even if not in request
  - `FilterReturnKeys_PreservesMatchingKeyValues` — tag with matching value is preserved
  - `FilterReturnKeys_IncludesReturnKeyValues` — tag with zero-length in request gets value from match
  - `FilterReturnKeys_EmptyRequest_ReturnsMinimal` — only QueryRetrieveLevel

  **MatchesWildcard tests** (~5 tests):
  - `MatchesWildcard_ExactMatch_ReturnsTrue`
  - `MatchesWildcard_AsteriskPrefix_Matches` — "*Smith" matches "John Smith"
  - `MatchesWildcard_CaseInsensitive_ForPN` — "smith*" matches "SMITH^JOHN"
  - `MatchesWildcard_CaseSensitive_ForNonPN` — "CT" does not match "ct" in case-sensitive mode
  - `MatchesWildcard_QuestionMark_MatchesSingleChar`

  Use `[TestFixture]` and `[Test]` attributes. Follow existing test patterns from the codebase (NUnit 4.x style with `Assert.That`).
  </action>
  <verify>
  `dotnet test tests/SharpDicom.Tests --filter "FullyQualifiedName~DicomQueryMatcher"` — all tests pass.
  `dotnet test tests/SharpDicom.Tests` — all tests pass including existing tests.
  </verify>
  <done>
  ~28 unit tests for DicomQueryMatcher and DicomDateRange covering wildcard translation, date range parsing, return key filtering, and in-memory matching.
  All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: SCP roundtrip integration tests and FileSystemDicomStore tests</name>
  <files>
    tests/SharpDicom.Tests/Network/ScpIntegrationTests.cs
    tests/SharpDicom.Tests/Storage/FileSystemDicomStoreTests.cs
  </files>
  <action>
  **ScpIntegrationTests** (`tests/SharpDicom.Tests/Network/ScpIntegrationTests.cs`):
  End-to-end tests using DicomServer + DicomClient. Use ephemeral ports (port 0 or high random ports) and localhost to avoid conflicts. Use a helper method to find a free port.

  Tests (~10 tests):
  - `CFindScp_WithCallback_ReturnsMatchingResults`: Start server with OnCFind returning 3 hardcoded datasets. Connect client, perform C-FIND query via CFindScu, assert 3 results received.
  - `CFindScp_WithNoHandler_ReturnsFailureStatus`: Start server without OnCFind. Client sends C-FIND, expects failure status 0xA900.
  - `CFindScp_ReturnKeysFiltered`: Start server with OnCFind returning datasets with many tags. Client requests specific tags. Assert response only contains requested tags.
  - `CFindScp_WildcardPatientName_MatchesCorrectly`: OnCFind callback uses DicomQueryMatcher to filter. Query with "Smith*" pattern. Assert only Smith patients returned.
  - `CStoreThenCFind_Roundtrip`: Store a file via C-STORE SCP (using OnCStoreRequest callback that saves to dictionary). Then C-FIND the same study. Assert the stored file's metadata is returned.
  - `CMoveScpStub_WithNoHandler_ReturnsFailure`: Start server without C-MOVE handlers. Send C-MOVE request, expect 0xA900.
  - `CGetScpStub_WithNoHandler_ReturnsFailure`: Same for C-GET.

  If C-MOVE/C-GET full roundtrip tests are complex (require multi-server setup), mark them as `[Explicit]` or use simplified callbacks that return test data.

  Advanced tests (if context permits):
  - `CMoveRoundtrip_ForwardsToDestination`: Start 2 servers (SCP with C-MOVE and destination SCP). Client sends C-MOVE. Assert destination received the file. Mark [Explicit] if too complex.
  - `CGetRoundtrip_ReceivesOnSameAssociation`: Client sends C-GET, receives C-STORE sub-ops back.

  Helper methods:
  - `CreateTestDataset(string patientName, string patientId, string studyUid, string modality)` — builds a minimal DicomDataset for testing
  - `GetFreePort()` — finds an available TCP port

  Use presentation contexts for both Verification (C-ECHO) and Q/R Find/Move/Get SOP Classes (Patient Root).

  **FileSystemDicomStoreTests** (`tests/SharpDicom.Tests/Storage/FileSystemDicomStoreTests.cs`):
  Tests using temp directories (clean up in TearDown).

  Tests (~8 tests):
  - `StoreAndRetrieve_RoundTrip`: Store a DicomDataset, retrieve by SOPInstanceUID, assert file exists and contains correct data.
  - `Store_CreatesHierarchicalDirectories`: Store file, assert patient_id/study_uid/series_uid/sop_uid.dcm structure exists.
  - `FindAsync_ByPatientName_ReturnsMatch`: Store 3 files, query by patient name, assert correct matches.
  - `FindAsync_ByStudyDate_ReturnsMatch`: Store with different dates, query by date range, assert correct matches.
  - `FindAsync_ByModality_ReturnsMatch`: Store CT and MR, query for CT only.
  - `FindAsync_NoMatch_ReturnsEmpty`: Query for non-existent patient.
  - `GetInstanceCount_ReturnsCorrectCount`: Store 5 files, assert count is 5.
  - `CreateServerOptions_WiresCallbacks`: Call CreateServerOptions, assert OnCFind, OnCStoreRequest are not null.

  Use `[SetUp]`/`[TearDown]` to create and clean temp directories. Use `Path.GetTempPath()` + random folder name.

  For storing test DicomDatasets that include proper File Meta Information, either:
  - Create minimal DicomFile objects with required tags
  - Or call the store's StoreAsync directly with a CStoreRequestContext and dataset
  </action>
  <verify>
  `dotnet test tests/SharpDicom.Tests` — all tests pass (new and existing).
  Count of new tests: ~18 in ScpIntegrationTests + FileSystemDicomStoreTests + DicomQueryMatcherTests (from Task 1) = ~46 total new tests.
  </verify>
  <done>
  ~18 integration/storage tests plus ~28 unit tests from Task 1.
  C-FIND SCP roundtrip verified end-to-end.
  FileSystemDicomStore storage, indexing, and querying verified.
  Return key filtering verified.
  All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` — zero warnings, zero errors
2. `dotnet test tests/SharpDicom.Tests` — all tests pass (existing + new)
3. C-FIND roundtrip test: client receives matching results from server
4. FileSystemDicomStore: store + find roundtrip works
5. DicomQueryMatcher: all wildcard/date range/filter tests pass
6. No test uses hardcoded ports that could conflict
</verification>

<success_criteria>
- ~40+ new tests covering all Phase 24 functionality
- C-FIND SCP end-to-end roundtrip verified
- FileSystemDicomStore storage and querying verified
- All wildcard/date range edge cases tested
- Return key filtering conformance verified
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/24-server-side-dimse/24-04-SUMMARY.md`
</output>
