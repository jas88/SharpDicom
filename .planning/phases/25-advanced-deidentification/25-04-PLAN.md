---
phase: 25-advanced-deidentification
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - tests/SharpDicom.Tests/Deidentification/UidReferenceWalkerTests.cs
  - tests/SharpDicom.Tests/Deidentification/OcrScannerTests.cs
  - tests/SharpDicom.Tests/Deidentification/OcrScannerOptionsTests.cs
  - tests/SharpDicom.Tests/Deidentification/AdvancedDeidentificationIntegrationTests.cs
autonomous: true

must_haves:
  truths:
    - "UidReferenceWalker remaps UIDs in nested sequences at arbitrary depth"
    - "Standard DICOM UIDs are never remapped by UidReferenceWalker"
    - "Multi-valued UIDs are remapped per component"
    - "OcrScanner configuration options are validated"
    - "Allow/deny list filtering works correctly"
    - "Pipeline integration ties OCR scanning and UID walking together"
    - "Pipeline order is OCR -> primary de-id -> date shift -> UID walk -> markers"
  artifacts:
    - path: "tests/SharpDicom.Tests/Deidentification/UidReferenceWalkerTests.cs"
      provides: "Unit tests for UID reference walking"
      min_lines: 100
    - path: "tests/SharpDicom.Tests/Deidentification/OcrScannerTests.cs"
      provides: "Unit tests for OCR scanner (stub mode and pixel preparation)"
      min_lines: 80
    - path: "tests/SharpDicom.Tests/Deidentification/OcrScannerOptionsTests.cs"
      provides: "Unit tests for OCR options and allowlist filtering"
      min_lines: 60
    - path: "tests/SharpDicom.Tests/Deidentification/AdvancedDeidentificationIntegrationTests.cs"
      provides: "Integration tests for combined UID walking and OCR pipeline"
      min_lines: 80
  key_links:
    - from: "UidReferenceWalkerTests"
      to: "UidReferenceWalker"
      via: "direct instantiation with UidRemapper"
      pattern: "new UidReferenceWalker"
    - from: "AdvancedDeidentificationIntegrationTests"
      to: "DicomDeidentifierBuilder"
      via: "WithUidReferenceWalking and WithOcrScanner"
      pattern: "WithUidReferenceWalking|WithOcrScanner"
---

<objective>
Create comprehensive test suite for Phase 25 advanced de-identification features: UidReferenceWalker, OcrScanner, and pipeline integration.

Purpose: Verify that UID reference walking correctly remaps all VR=UI elements across sequences, that OCR scanner configuration and filtering work as designed, and that the combined pipeline integrates both features into DicomDeidentifier.

Output: ~40-60 NUnit tests covering unit, integration, and edge cases for both deliverables.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-advanced-deidentification/25-01-SUMMARY.md
@.planning/phases/25-advanced-deidentification/25-02-SUMMARY.md
@.planning/phases/25-advanced-deidentification/25-03-SUMMARY.md

@src/SharpDicom/Deidentification/UidReferenceWalker.cs
@src/SharpDicom/Deidentification/OcrScanner.cs
@src/SharpDicom/Deidentification/OcrScannerOptions.cs
@src/SharpDicom/Deidentification/OcrScanResult.cs
@src/SharpDicom/Deidentification/DicomDeidentifier.cs
@src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs
@tests/SharpDicom.Tests/Deidentification/DicomDeidentifierTests.cs
@tests/SharpDicom.Tests/Deidentification/UidRemapperTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: UidReferenceWalker unit tests</name>
  <files>
    tests/SharpDicom.Tests/Deidentification/UidReferenceWalkerTests.cs
  </files>
  <action>
Create NUnit test class UidReferenceWalkerTests in namespace SharpDicom.Tests.Deidentification.

Test categories using [Category("Deidentification")]:

**Core remapping tests:**
1. RemapAllReferences_EmptyDataset_ReturnsZeroCounts -- empty dataset returns UidsRemapped=0, SequenceItemsTraversed=0
2. RemapAllReferences_TopLevelUiElement_RemapsUid -- dataset with a single VR=UI element (e.g., ReferencedSOPInstanceUID) is remapped
3. RemapAllReferences_StandardUid_NotRemapped -- dataset containing Transfer Syntax UID (1.2.840.10008.*) is preserved
4. RemapAllReferences_NonUiElement_Untouched -- dataset with VR=LO, PN, DA elements is not modified
5. RemapAllReferences_MultiValuedUid_RemapsEachComponent -- backslash-separated UIDs (e.g., "1.2.3\\1.2.4\\1.2.5") are each remapped independently. Verify that the output contains backslash-separated remapped UIDs and that UidsRemapped count reflects the number of components remapped.

**Sequence traversal tests:**
6. RemapAllReferences_SingleLevelSequence_RemapsUidsInItems -- sequence with items containing VR=UI elements
7. RemapAllReferences_NestedSequences_RemapsAtAllDepths -- 3-level deep nesting (sequence within sequence within sequence) with UIDs at each level
8. RemapAllReferences_DeepNesting_HandlesArbitraryDepth -- 10-level deep nesting to verify no stack overflow or depth limit
9. RemapAllReferences_MultipleItemsInSequence_RemapsEach -- sequence with 5 items, each containing different UIDs
10. RemapAllReferences_MixedSequencesAndUids_CorrectCounts -- dataset with UIDs at top level and in sequences, verify counts match

**Consistency tests:**
11. RemapAllReferences_SameUidInMultipleLocations_MapsConsistently -- same UID appearing in multiple sequence items gets same remapped value
12. RemapAllReferences_ConsistentWithPriorMapping_UsesCachedMapping -- pre-populate UidRemapper with a mapping, verify walker uses it
13. RemapAllReferences_CalledTwice_NoDoubleRemapping -- calling RemapAllReferences on already-remapped dataset doesn't change UIDs (they're already in the new namespace)

**RT/SR reference pattern tests:**
14. RemapAllReferences_RtPlanReferences_RemapsReferencedSopInstanceUid -- dataset mimicking RT Plan with ReferencedBeamSequence > ReferencedSOPInstanceUID
15. RemapAllReferences_StructuredReportReferences_RemapsAllReferences -- dataset mimicking SR with CurrentRequestedProcedureEvidenceSequence
16. RemapAllReferences_ReferencedFrameOfReferenceUid_Remapped -- tag (0020,0052) FrameOfReferenceUID in sequences is remapped

**Result type tests:**
17. UidRemapResult_RemappedTags_ContainsAffectedTags -- verify RemappedTags list includes correct DicomTags
18. UidRemapResult_SequenceItemsTraversed_CountsCorrectly -- verify count matches actual items visited

For creating test datasets: use `new DicomDataset()` and `dataset.Add()` with `DicomStringElement` for VR=UI elements and `DicomSequence` for sequences. Follow the patterns already established in DicomDeidentifierTests.cs and UidRemapperTests.cs.
  </action>
  <verify>
`dotnet test tests/SharpDicom.Tests/SharpDicom.Tests.csproj --filter "FullyQualifiedName~UidReferenceWalkerTests"` -- all tests pass.
  </verify>
  <done>18 unit tests covering UID reference walking: core remapping, multi-valued UIDs, sequence traversal, consistency, RT/SR patterns, and result types.</done>
</task>

<task type="auto">
  <name>Task 2: OcrScanner tests and integration tests</name>
  <files>
    tests/SharpDicom.Tests/Deidentification/OcrScannerTests.cs
    tests/SharpDicom.Tests/Deidentification/OcrScannerOptionsTests.cs
    tests/SharpDicom.Tests/Deidentification/AdvancedDeidentificationIntegrationTests.cs
  </files>
  <action>
**OcrScannerOptionsTests.cs:**
Tests for configuration and filtering logic (no Tesseract dependency):

1. DefaultNonPhiAllowlist_ContainsOrientationMarkers -- L, R, P, A, S, I, H, F present
2. DefaultNonPhiAllowlist_ContainsMeasurementUnits -- cm, mm, Hz, bpm, etc. present
3. DefaultNonPhiAllowlist_DoesNotContainNames -- "John", "Smith", etc. not present
4. OcrScanModality_HighRiskAndModerateRisk_IsDefault -- verify default value
5. ConfidenceThreshold_DefaultIs0Point6 -- verify default
6. EdgeConfidenceThreshold_DefaultIs0Point4 -- verify default
7. PageSegMode_DefaultIs11 -- PSM_SPARSE_TEXT
8. MaxDetectionsPerFrame_DefaultIs200
9. DecompressForOcr_DefaultIsTrue -- verify default

**OcrScannerTests.cs:**
Tests for OcrScanner behavior (most will test without actual Tesseract):

10. Constructor_WhenTesseractUnavailable_ThrowsInvalidOperation -- since we build in stub mode, tess_available() returns 0, constructor should throw. Verify the exception message is descriptive.
11. OcrScanResult_ToRedactionRegions_ConvertsDetections -- create an OcrScanResult manually, verify ToRedactionRegions() returns correct RedactionRegion list
12. OcrScanResult_ToRedactionRegions_EmptyDetections_ReturnsEmpty
13. OcrScanResult_FilteredDetections_ExcludesAllowlisted -- manually constructed result with "L" and "John Smith" detections, verify filtering
14. OcrDetection_IsEdgeRegion_TrueForCorner -- detection at (5, 5) in 512x512 image with 15% margin
15. OcrDetection_IsEdgeRegion_FalseForCenter -- detection at (250, 250) in 512x512 image

If Tesseract IS available (mark these with [Explicit] category for manual/local-only verification):
16. [Explicit] ScanDataset_WithBurnedInText_DetectsText -- create a synthetic 256x256 8-bit dataset with white text on black background, verify detection
17. [Explicit] ScanDataset_16BitImage_WindowsTo8Bit -- verify 16-bit data is handled without crash
18. [Explicit] ScanDataset_Monochrome1_InvertsBeforeOcr -- verify MONOCHROME1 handling

**AdvancedDeidentificationIntegrationTests.cs:**
Integration tests for the full pipeline:

19. Builder_WithUidReferenceWalking_CreatesDeidentifier -- verify builder.WithUidReferenceWalking().Build() succeeds
20. Builder_WithOcrScanner_CreatesDeidentifier -- verify builder.WithOcrScanner().Build() throws (Tesseract unavailable in CI) with correct exception type
21. Deidentify_WithUidReferenceWalking_RemapsSequenceUids -- create dataset with referenced UIDs in sequences, de-identify with WithUidReferenceWalking(), verify UIDs in sequences are remapped
22. Deidentify_WithUidReferenceWalking_PreservesStandardUids -- verify Transfer Syntax UID, SOP Class UID unchanged after de-identification with reference walking
23. Deidentify_WithUidReferenceWalking_ConsistentAcrossFiles -- de-identify two datasets with same referenced UID, verify both get same remapped value
24. Deidentify_WithoutUidReferenceWalking_SequenceUidsUnchanged -- verify backward compatibility: without WithUidReferenceWalking(), sequence UIDs are NOT walked (only PS3.15 profile actions apply)
25. Deidentify_WithUidReferenceWalking_ResultIncludesStats -- verify DeidentificationResult includes reference walking statistics
26. Deidentify_PipelineOrder_OcrBeforePrimaryDeid -- verify that OCR scanning happens before primary de-identification by checking that modality-dependent OCR behavior works correctly (e.g., US modality triggers scan)
27. Deidentify_WithUidReferenceWalking_MultiValuedUids_RemappedPerComponent -- verify multi-valued UIDs in sequences are handled correctly through the pipeline

For pipeline tests that need DicomDeidentifier: use `new DicomDeidentifierBuilder().WithBasicProfile().WithUidReferenceWalking().Build()` pattern.

All tests use NUnit [Test] attribute, [Category("Deidentification")], and follow existing test patterns in the tests/SharpDicom.Tests/Deidentification/ directory.

Important: Tests MUST compile and pass even when the Tesseract native library is not available. OCR-dependent tests should either test the "unavailable" path (expected exception) or be marked [Explicit] for manual verification with Tesseract installed.
  </action>
  <verify>
`dotnet test tests/SharpDicom.Tests/SharpDicom.Tests.csproj --filter "FullyQualifiedName~OcrScanner|FullyQualifiedName~AdvancedDeidentification"` -- all non-Explicit tests pass. `dotnet test tests/SharpDicom.Tests/SharpDicom.Tests.csproj` -- full suite passes (2159+ existing + ~27 new tests).
  </verify>
  <done>~27 tests covering OCR scanner options, scanner behavior (stub mode), integration pipeline with UID reference walking, multi-valued UIDs, pipeline order verification, and backward compatibility.</done>
</task>

</tasks>

<verification>
1. `dotnet build` -- entire solution compiles with zero warnings
2. `dotnet test` -- all tests pass (existing 2159+ plus new ~45 tests)
3. No test depends on Tesseract being installed (Explicit tests excluded from normal run)
4. UidReferenceWalker tests cover: empty, top-level, nested, deep, multi-valued, consistency, RT/SR patterns
5. OcrScanner tests verify fail-fast behavior, result conversion, and allow/deny filtering
6. Integration tests verify pipeline with and without advanced features
7. Pipeline order test confirms OCR runs before primary de-identification
</verification>

<success_criteria>
- 45+ new tests added, all passing
- Total test count: 2200+ (2159 existing + 45+ new)
- Zero test failures
- OCR tests work without Tesseract native library installed
- UID reference walking tests prove recursive traversal at arbitrary depth
- Multi-valued UID tests prove per-component handling
- Integration tests prove pipeline backward compatibility and correct ordering
</success_criteria>

<output>
After completion, create `.planning/phases/25-advanced-deidentification/25-04-SUMMARY.md`
</output>
