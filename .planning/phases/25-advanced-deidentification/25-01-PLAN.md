---
phase: 25-advanced-deidentification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SharpDicom/Deidentification/UidReferenceWalker.cs
  - src/SharpDicom/Deidentification/UidRemapResult.cs
  - src/SharpDicom/Deidentification/DicomDeidentifier.cs
  - src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs
autonomous: true

must_haves:
  truths:
    - "All VR=UI elements across all sequence depths are remapped consistently"
    - "Standard DICOM UIDs (Transfer Syntax, SOP Class) are never remapped"
    - "UidReferenceWalker works standalone and via DicomDeidentifier pipeline"
    - "Multi-valued UIDs (backslash-separated) are handled per component"
  artifacts:
    - path: "src/SharpDicom/Deidentification/UidReferenceWalker.cs"
      provides: "Generic recursive VR=UI traversal for UID remapping in sequences"
      exports: ["UidReferenceWalker"]
    - path: "src/SharpDicom/Deidentification/UidRemapResult.cs"
      provides: "Result type for reference walking"
      exports: ["UidRemapResult"]
  key_links:
    - from: "UidReferenceWalker"
      to: "UidRemapper"
      via: "constructor injection"
      pattern: "new UidReferenceWalker\\(.*remapper"
    - from: "DicomDeidentifier.Deidentify"
      to: "UidReferenceWalker.RemapAllReferences"
      via: "post-processing step after primary de-identification and date shifting, before markers"
      pattern: "_referenceWalker.*RemapAllReferences"
---

<objective>
Implement UidReferenceWalker for comprehensive referenced SOP Instance UID updates across all sequences, and integrate it into the DicomDeidentifier pipeline.

Purpose: De-identification requires all cross-referenced UIDs (RT Plan, Presentation State, Structured Report, Key Object Selection, etc.) to be consistently remapped. The existing UidRemapper.RemapDataset() only handles top-level tags marked with RemapUid action by PS3.15 profiles. UidReferenceWalker performs a generic VR=UI traversal at unlimited depth, catching ALL UID references including those in nested sequences.

Output: UidReferenceWalker class, UidRemapResult type, and pipeline integration in DicomDeidentifier.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/SharpDicom/Deidentification/UidRemapper.cs
@src/SharpDicom/Deidentification/DicomDeidentifier.cs
@src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs
@src/SharpDicom/Deidentification/DeidentificationResult.cs
@src/SharpDicom/Data/DicomDataset.cs
@src/SharpDicom/Data/DicomSequence.cs
@src/SharpDicom/Data/DicomVR.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: UidReferenceWalker and UidRemapResult types</name>
  <files>
    src/SharpDicom/Deidentification/UidReferenceWalker.cs
    src/SharpDicom/Deidentification/UidRemapResult.cs
  </files>
  <action>
Create UidRemapResult as a simple result class:
- UidsRemapped (int) -- count of UIDs that were remapped
- SequenceItemsTraversed (int) -- count of sequence items visited
- RemappedTags (List of DicomTag) -- specific tags that were remapped (for diagnostics)

Create UidReferenceWalker as a sealed class:
- Constructor takes UidRemapper (required, not owned)
- Public method: UidRemapResult RemapAllReferences(DicomDataset dataset, string? context = null)
- Private method: void WalkDataset(DicomDataset dataset, string? context, UidRemapResult result)

WalkDataset algorithm:
1. Collect all tags from dataset into a list (avoid modifying during enumeration, same pattern as existing DicomDeidentifier.ProcessDataset)
2. For each element:
   a. If DicomSequence: recurse into each item, increment SequenceItemsTraversed
   b. If VR == DicomVR.UI and element is DicomStringElement:
      - Get string value via GetString(DicomEncoding.Default)
      - Skip if null/whitespace
      - Handle multi-valued UIDs: split on backslash ('\\') to get individual UID components. For each component:
        - Trim the component
        - Skip if UidRemapper.IsStandardUid(trimmedComponent) returns true (preserves Transfer Syntax UIDs, SOP Class UIDs, etc.)
        - Call UidRemapper.Remap(trimmedComponent, context) to get new UID
      - If ANY component was remapped, reconstruct the full string by joining all components (remapped or original) with backslash, create new DicomStringElement with the remapped bytes, and add to dataset
      - Increment UidsRemapped by the number of components that changed, add tag to RemappedTags
   c. Otherwise: skip (non-UI elements untouched)

Important details:
- Use System.Text.Encoding.ASCII.GetBytes for UID encoding (UIDs are always ASCII)
- The walker does NOT skip any tags (unlike UidRemapper.RemapDataset which skips tags based on PS3.15 profile). It remaps ALL VR=UI elements.
- This makes it handle all current and future referencing patterns including RT Plan, Presentation State, SR, KOS, private sequences, etc.
- Follow the netstandard2.0 + net8.0 + net9.0 multi-targeting patterns already in the codebase
- Include full XML doc comments following project conventions
- Namespace: SharpDicom.Deidentification
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj` compiles with zero warnings.
  </verify>
  <done>UidReferenceWalker class exists with RemapAllReferences method that recursively walks all sequences remapping VR=UI elements, including multi-valued (backslash-separated) UIDs handled per component. UidRemapResult provides traversal statistics.</done>
</task>

<task type="auto">
  <name>Task 2: DicomDeidentifier and Builder pipeline integration</name>
  <files>
    src/SharpDicom/Deidentification/DicomDeidentifier.cs
    src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs
  </files>
  <action>
Modify DicomDeidentifier:
1. Add a private readonly field: `private readonly UidReferenceWalker? _referenceWalker;`
2. Update constructor to accept optional UidReferenceWalker parameter (or derive from existing UidRemapper):
   - Add parameter `bool walkAllUidReferences = false` OR accept a `UidReferenceWalker?` directly
   - Preferred approach: Accept a bool flag `walkAllUidReferences`. When true, create a UidReferenceWalker using the existing _uidRemapper.
   - This keeps the constructor simple and avoids exposing UidReferenceWalker as a required dependency.
3. In the Deidentify method, the pipeline order MUST be:
   1. ProcessDataset (primary de-identification)
   2. Date shifting (if configured)
   3. UID reference walking (if enabled) -- AFTER primary de-identification so UidRemapper has seen primary UIDs
   4. AddDeidentificationMarkers -- ALWAYS last

   Specifically, AFTER date shifting and BEFORE AddDeidentificationMarkers:
   - If _referenceWalker is not null, call `_referenceWalker.RemapAllReferences(dataset)`.
   - Add the result stats to DeidentificationResult (add a new field `UidReferencesRemapped` to DeidentificationResult or accumulate into the existing UidsRemapped count).

The key design insight: The reference walker runs AFTER primary de-identification so that the UidRemapper has already seen and mapped the primary UIDs (SOPInstanceUID, StudyInstanceUID, etc.). When the walker encounters those same UIDs in reference sequences, the UidRemapper returns the same consistent mapping.

Modify DicomDeidentifierBuilder:
1. Add `private bool _walkAllUidReferences;` field
2. Add `public DicomDeidentifierBuilder WithUidReferenceWalking()` method that sets `_walkAllUidReferences = true` and returns `this`
3. In Build(), pass the flag to DicomDeidentifier constructor

Also update DeidentificationResult.Summary if needed:
- Add `int UidReferencesRemapped` property to the summary type (or reuse existing UidsRemapped counter)

All changes must maintain backward compatibility -- existing callers that don't use WithUidReferenceWalking() should behave identically to current behavior.

NOTE: Plan 25-03 will also modify DicomDeidentifier.Deidentify() to add OCR scanning. The definitive pipeline order is:
  1. OCR scan + pixel redaction (Plan 25-03 adds this)
  2. ProcessDataset (primary de-identification)
  3. Date shifting
  4. UID reference walking (this plan adds this)
  5. AddDeidentificationMarkers
Plan 25-03 depends on 25-02, and 25-04 depends on both 25-01 and 25-03. Since Plans 01 and 03 both modify DicomDeidentifier, the executor of 25-03 must extend the changes made here.
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj` compiles with zero warnings. `dotnet test tests/SharpDicom.Tests/SharpDicom.Tests.csproj` -- all existing tests still pass.
  </verify>
  <done>DicomDeidentifierBuilder.WithUidReferenceWalking() enables comprehensive UID reference walking in the pipeline. All existing 2159+ tests still pass.</done>
</task>

</tasks>

<verification>
1. `dotnet build` -- entire solution compiles with zero warnings
2. `dotnet test` -- all existing tests pass (2159+ expected)
3. UidReferenceWalker can be instantiated standalone: `new UidReferenceWalker(remapper)`
4. Pipeline integration works: `new DicomDeidentifierBuilder().WithBasicProfile().WithUidReferenceWalking().Build()`
</verification>

<success_criteria>
- UidReferenceWalker recursively remaps all VR=UI elements at unlimited depth
- Multi-valued UIDs (backslash-separated) are split and each component remapped independently
- Standard DICOM UIDs (1.2.840.10008.*) are preserved
- DicomDeidentifier pipeline integration is opt-in and backward-compatible
- Pipeline order: ProcessDataset -> date shifting -> UID reference walking -> markers
- Solution builds with zero warnings and all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-advanced-deidentification/25-01-SUMMARY.md`
</output>
