---
phase: 23-cli-tools
plan: 06
type: execute
wave: 3
depends_on: ["23-02", "23-03", "23-04", "23-05"]
files_modified:
  - tests/SharpDicom.Tests/Cli/DumpCommandTests.cs
  - tests/SharpDicom.Tests/Cli/LintCommandTests.cs
  - tests/SharpDicom.Tests/Cli/FixCommandTests.cs
  - tests/SharpDicom.Tests/Cli/ConfigLoaderTests.cs
  - tests/SharpDicom.Tests/Cli/FileEnumeratorTests.cs
  - tests/SharpDicom.Tests/Cli/ConnectionStringParserTests.cs
autonomous: false

must_haves:
  truths:
    - "All 5 subcommands respond to --help with correct syntax"
    - "dump produces correct text, JSON, and XML output for a known DICOM file"
    - "lint returns exit code 3 for files with validation errors"
    - "lint returns exit code 0 for valid files"
    - "fix --dry-run reports changes without modifying files"
    - "fix creates .fixed.dcm output files"
    - "Config loader reads TOML, applies env var overrides"
    - "File enumerator discovers .dcm files recursively"
    - "Connection string parser handles pacs://AET@host:port"
    - "All existing tests still pass (no regressions)"
  artifacts:
    - path: "tests/SharpDicom.Tests/Cli/DumpCommandTests.cs"
      provides: "Tests for dump command output formats"
      min_lines: 40
    - path: "tests/SharpDicom.Tests/Cli/LintCommandTests.cs"
      provides: "Tests for lint command exit codes and output"
      min_lines: 40
    - path: "tests/SharpDicom.Tests/Cli/FixCommandTests.cs"
      provides: "Tests for fix command repairs and dry-run"
      min_lines: 40
    - path: "tests/SharpDicom.Tests/Cli/ConfigLoaderTests.cs"
      provides: "Tests for config loading and precedence"
      min_lines: 30
    - path: "tests/SharpDicom.Tests/Cli/FileEnumeratorTests.cs"
      provides: "Tests for file discovery"
      min_lines: 20
    - path: "tests/SharpDicom.Tests/Cli/ConnectionStringParserTests.cs"
      provides: "Tests for connection string parsing"
      min_lines: 20
  key_links:
    - from: "tests/SharpDicom.Tests/Cli/DumpCommandTests.cs"
      to: "src/SharpDicom.Cli/Commands/DumpCommand.cs"
      via: "Test exercises dump output"
      pattern: "DumpCommand|TextFormatter|JsonFormatter"
    - from: "tests/SharpDicom.Tests/Cli/LintCommandTests.cs"
      to: "src/SharpDicom.Cli/Commands/LintCommand.cs"
      via: "Test exercises lint validation"
      pattern: "LintCommand|ValidationProfile"
---

<objective>
Write integration and unit tests for the CLI infrastructure and commands, then verify the full system with a human checkpoint.

Purpose: Ensure all CLI components work correctly, produce expected output, and handle error cases properly. Tests exercise the commands programmatically (not via process invocation) by testing the formatters, fix engine, config loader, and helpers directly.

Output: Test suite covering core CLI functionality, plus human verification of end-to-end CLI experience.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-cli-tools/23-CONTEXT.md
@.planning/phases/23-cli-tools/23-01-SUMMARY.md
@.planning/phases/23-cli-tools/23-02-SUMMARY.md
@.planning/phases/23-cli-tools/23-03-SUMMARY.md
@.planning/phases/23-cli-tools/23-04-SUMMARY.md
@.planning/phases/23-cli-tools/23-05-SUMMARY.md
@src/SharpDicom.Cli/Output/TextFormatter.cs
@src/SharpDicom.Cli/Output/JsonFormatter.cs
@src/SharpDicom.Cli/Output/XmlFormatter.cs
@src/SharpDicom.Cli/Diagnostics/DicomFixer.cs
@src/SharpDicom.Cli/Configuration/ConfigLoader.cs
@src/SharpDicom.Cli/Helpers/FileEnumerator.cs
@src/SharpDicom.Cli/Helpers/ConnectionStringParser.cs
@src/SharpDicom.Cli/Helpers/ExitCodes.cs
@tests/SharpDicom.Tests/SharpDicom.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for CLI helpers and formatters</name>
  <files>
    tests/SharpDicom.Tests/Cli/ConfigLoaderTests.cs
    tests/SharpDicom.Tests/Cli/FileEnumeratorTests.cs
    tests/SharpDicom.Tests/Cli/ConnectionStringParserTests.cs
    tests/SharpDicom.Tests/Cli/DumpCommandTests.cs
    tests/SharpDicom.Tests/SharpDicom.Tests.csproj
  </files>
  <action>
First, add a project reference to SharpDicom.Cli in SharpDicom.Tests.csproj:
```xml
<ProjectReference Include="..\..\src\SharpDicom.Cli\SharpDicom.Cli.csproj" />
```

**ConnectionStringParserTests.cs:**
- Test valid: `pacs://ORTHANC@localhost:11112` -> host=localhost, port=11112, calledAe=ORTHANC
- Test valid without port: `pacs://MY_AET@dicom.example.com` -> port defaults to 104
- Test valid with IP: `pacs://AET@192.168.1.100:4242`
- Test invalid: empty string, no scheme, missing AE, malformed -> returns false
- Test port out of range: `pacs://AET@host:99999` -> returns false

**ConfigLoaderTests.cs:**
- Test loading defaults when no config file exists
- Test loading a TOML config file with known content (write temp file, load, verify):
  ```toml
  output_format = "json"
  verbosity = "verbose"

  [profiles.orthanc]
  host = "localhost"
  port = 4242
  called_ae = "ORTHANC"
  ```
- Test environment variable override: set SHARPDCM_OUTPUT_FORMAT, verify it overrides file config
- Test malformed TOML file returns defaults (not throws)

**FileEnumeratorTests.cs:**
- Create temp directory with .dcm and .txt files, verify only .dcm returned
- Create nested directories with .dcm files, verify recursive discovery
- Test non-existent path throws FileNotFoundException
- Test single file input returns that file

**DumpCommandTests.cs** (tests formatters, not full command):
- Create a DicomDataset programmatically with known elements (PatientName, StudyDate, Modality)
- Test TextFormatter outputs `(0010,0010) PN [Smith^John] # PatientName` format
- Test JsonFormatter outputs valid JSON with tag, vr, keyword, value fields:
  - Write to StringWriter, parse result with System.Text.Json, verify structure
- Test XmlFormatter outputs well-formed XML:
  - Write to StringWriter, parse with XDocument, verify elements
- Test sequence formatting: create dataset with a sequence, verify nesting in text output
- Test pixel data summarization: element with OW VR at (7FE0,0010) shows length not data

All tests use NUnit `[Test]` and `[TestFixture]` attributes consistent with existing test conventions.
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Tests/ --filter "FullyQualifiedName~Cli"` passes all new tests.
    `dotnet test tests/SharpDicom.Tests/` passes (no regressions).
  </verify>
  <done>
    Unit tests for ConnectionStringParser, ConfigLoader, FileEnumerator, and output formatters all pass. No regressions in existing tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for lint, fix, and DicomFixer engine</name>
  <files>
    tests/SharpDicom.Tests/Cli/LintCommandTests.cs
    tests/SharpDicom.Tests/Cli/FixCommandTests.cs
  </files>
  <action>
**LintCommandTests.cs:**
- Create a valid DICOM dataset programmatically (all required Type 1 elements), save to temp file
- Lint with Strict profile -> no errors -> assert result count is 0 or all Info/Warning
- Create a dataset with intentionally invalid UID (e.g., "not.a.valid.uid..with..double.dots"), save to temp file
- Lint with Strict profile -> should find validation errors
- Test JSON output format: validate that the output is parseable JSON with expected structure
- Test that lint summary counts match (errors, warnings, infos)

**FixCommandTests.cs:**
- Test DicomFixer.Fix with a dataset containing invalid UID:
  - Create dataset with element (0020,000D) StudyInstanceUID = "1.2.3.04.5" (leading zero in component)
  - Call DicomFixer.Fix with FixInvalidUids=true
  - Assert FixAction returned with old/new values
  - Assert new UID is valid (matches UID format)
- Test DicomFixer.Fix with invalid date:
  - Create dataset with (0008,0020) StudyDate = "2024.01.15"
  - Call fix -> assert new value is "20240115"
- Test DicomFixer.Fix with missing SpecificCharacterSet:
  - Create dataset with string element containing non-ASCII bytes but no (0008,0005)
  - Call fix -> assert (0008,0005) added with "ISO_IR 100"
- Test dry-run behavior (fix engine returns actions but dataset not saved)
- Test output path generation: "file.dcm" -> "file.fixed.dcm"
- Test RemoveInvalidElements: create element that fails validation, fix with remove option, verify element gone

All tests:
- Use temp directories and files, cleaned up in [TearDown]
- Are self-contained (no external dependencies)
- Follow existing test patterns in the codebase
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Tests/ --filter "FullyQualifiedName~Cli"` passes all new tests.
    `dotnet test tests/SharpDicom.Tests/` passes all tests (no regressions).
    `dotnet test` at solution root passes (all projects).
  </verify>
  <done>
    Lint and fix tests all pass. DicomFixer engine tested for UID fix, date fix, encoding fix, and element removal. No regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete CLI toolkit with 5 subcommands (dump, store, find, lint, fix), output formatters (text, JSON, XML), configuration system, progress reporting, and test suite.
  </what-built>
  <how-to-verify>
    1. Run `dotnet run --project src/SharpDicom.Cli -- --help` and verify all 5 subcommands listed
    2. Run `dotnet run --project src/SharpDicom.Cli -- dump` on a DICOM file and verify readable output
    3. Run `dotnet run --project src/SharpDicom.Cli -- dump file.dcm --format json | python3 -m json.tool` to verify valid JSON
    4. Run `dotnet run --project src/SharpDicom.Cli -- lint file.dcm` and verify validation output
    5. Run `dotnet run --project src/SharpDicom.Cli -- fix file.dcm --dry-run` and verify change report
    6. Run `dotnet test` and verify all tests pass
    7. Check that progress output goes to stderr (pipe dump output and verify no progress bar in piped content)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `dotnet test` passes all tests including new CLI tests
- `dotnet test --filter "FullyQualifiedName~Cli"` passes all CLI-specific tests
- No regressions in existing 2025+ tests
- All 5 subcommands respond to --help
- dump produces valid text/JSON/XML output
- lint returns correct exit codes (0 valid, 3 invalid)
- fix --dry-run shows changes without modifying files
</verification>

<success_criteria>
- At least 15 new unit/integration tests covering CLI infrastructure
- ConnectionStringParser tests: valid, invalid, edge cases
- ConfigLoader tests: defaults, TOML loading, env var override, malformed file
- FileEnumerator tests: recursive discovery, filtering, error handling
- Formatter tests: text format, JSON validity, XML well-formedness
- DicomFixer tests: UID fix, date fix, encoding fix, element removal
- Lint tests: exit codes, output formats
- All existing tests pass without regression
- Human verification of end-to-end CLI experience
</success_criteria>

<output>
After completion, create `.planning/phases/23-cli-tools/23-06-SUMMARY.md`
</output>
