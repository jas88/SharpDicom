---
phase: 14-de-identification
plan: 07
type: execute
wave: 3
depends_on: ["14-04"]
files_modified:
  - src/SharpDicom/Deidentification/PixelCleaner/TesseractPhiDetector.cs
  - src/SharpDicom/SharpDicom.csproj
  - tests/SharpDicom.Tests/Deidentification/TesseractPhiDetectorTests.cs
autonomous: true
user_setup:
  - service: tesseract
    why: "OCR engine for burned-in PHI detection"
    env_vars: []
    dashboard_config:
      - task: "Download tessdata-best trained data for English"
        location: "https://github.com/tesseract-ocr/tessdata_best - download eng.traineddata"
      - task: "Set TESSDATA_PREFIX environment variable"
        location: "Point to directory containing eng.traineddata"

must_haves:
  truths:
    - "OCR can detect text in ultrasound image corners"
    - "TesseractPhiDetector implements IBurnedInPhiDetector interface"
    - "Detector returns bounding boxes for detected text regions"
    - "Detector is optional - system works without Tesseract installed"
  artifacts:
    - path: "src/SharpDicom/Deidentification/PixelCleaner/TesseractPhiDetector.cs"
      provides: "OCR-based PHI detection"
      exports: ["TesseractPhiDetector"]
    - path: "tests/SharpDicom.Tests/Deidentification/TesseractPhiDetectorTests.cs"
      provides: "OCR detector tests"
  key_links:
    - from: "TesseractPhiDetector.cs"
      to: "IBurnedInPhiDetector.cs"
      via: "interface implementation"
      pattern: "class TesseractPhiDetector.*:.*IBurnedInPhiDetector"
    - from: "TesseractPhiDetector.cs"
      to: "TesseractOCR package"
      via: "Tesseract engine wrapper"
      pattern: "using Tesseract|TesseractEngine"
---

<objective>
Implement TesseractPhiDetector as an OCR-based burned-in PHI detector that complements the heuristic detector for higher accuracy on text-heavy images like ultrasound.

Purpose: CONTEXT.md specifies hybrid OCR + heuristics approach. Research shows Tesseract is the standard open-source OCR solution. This provides higher accuracy detection for high-risk modalities (US, SC) where text is common.

Output: TesseractPhiDetector implementing IBurnedInPhiDetector with graceful fallback when Tesseract is not available
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-de-identification/14-CONTEXT.md
@.planning/phases/14-de-identification/14-RESEARCH.md
@.planning/phases/14-de-identification/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TesseractOCR NuGet package</name>
  <files>src/SharpDicom/SharpDicom.csproj</files>
  <action>
Add TesseractOCR package reference to SharpDicom.csproj:

```xml
<ItemGroup Condition="'$(TargetFramework)' != 'netstandard2.0'">
  <!-- TesseractOCR only supported on net6.0+ due to native dependencies -->
  <PackageReference Include="TesseractOCR" Version="5.5.1" />
</ItemGroup>
```

Note: TesseractOCR has native dependencies that may not work on netstandard2.0. Use conditional reference.

The package wraps Tesseract OCR engine and requires:
- tessdata folder with trained data (eng.traineddata for English)
- TESSDATA_PREFIX environment variable pointing to tessdata folder

Add conditional compilation symbol for Tesseract support:
```xml
<PropertyGroup Condition="'$(TargetFramework)' != 'netstandard2.0'">
  <DefineConstants>$(DefineConstants);TESSERACT_AVAILABLE</DefineConstants>
</PropertyGroup>
```
  </action>
  <verify>
`dotnet restore src/SharpDicom/SharpDicom.csproj`
`dotnet build src/SharpDicom/SharpDicom.csproj --framework net9.0`
  </verify>
  <done>TesseractOCR package added with conditional compilation for net6.0+</done>
</task>

<task type="auto">
  <name>Task 2: Create TesseractPhiDetector</name>
  <files>src/SharpDicom/Deidentification/PixelCleaner/TesseractPhiDetector.cs</files>
  <action>
Create OCR-based detector with graceful fallback:
```csharp
#if TESSERACT_AVAILABLE
using Tesseract;
#endif

namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// OCR-based PHI detector using Tesseract for text detection in pixel data.
/// Falls back to heuristic detection when Tesseract is not available.
/// </summary>
/// <remarks>
/// Requires:
/// - TesseractOCR NuGet package (net6.0+ only)
/// - tessdata folder with eng.traineddata
/// - TESSDATA_PREFIX environment variable
/// </remarks>
public sealed class TesseractPhiDetector : IBurnedInPhiDetector, IDisposable
{
#if TESSERACT_AVAILABLE
    private readonly TesseractEngine? _engine;
    private readonly HeuristicPhiDetector _fallback;
    private readonly float _confidenceThreshold;
    private bool _disposed;

    /// <summary>
    /// Creates a new Tesseract PHI detector.
    /// </summary>
    /// <param name="tessdataPath">Path to tessdata folder, or null to use TESSDATA_PREFIX env var.</param>
    /// <param name="confidenceThreshold">Minimum confidence (0-1) to include detected text. Default 0.6.</param>
    public TesseractPhiDetector(string? tessdataPath = null, float confidenceThreshold = 0.6f)
    {
        _confidenceThreshold = confidenceThreshold;
        _fallback = new HeuristicPhiDetector();

        try
        {
            var dataPath = tessdataPath
                ?? Environment.GetEnvironmentVariable("TESSDATA_PREFIX")
                ?? Path.Combine(AppContext.BaseDirectory, "tessdata");

            if (Directory.Exists(dataPath))
            {
                _engine = new TesseractEngine(dataPath, "eng", EngineMode.Default);
            }
        }
        catch
        {
            // Tesseract initialization failed - will use fallback
            _engine = null;
        }
    }

    /// <summary>
    /// Gets whether Tesseract OCR is available and initialized.
    /// </summary>
    public bool IsOcrAvailable => _engine != null;

    /// <inheritdoc />
    public async ValueTask<PhiDetectionResult> DetectAsync(
        ReadOnlyMemory<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel,
        string? modality,
        CancellationToken ct = default)
    {
        if (_engine == null)
        {
            // Fall back to heuristic detection
            return await _fallback.DetectAsync(
                pixelData, width, height, bitsAllocated, samplesPerPixel, modality, ct);
        }

        // Convert to 8-bit grayscale for Tesseract
        var grayscale = ConvertToGrayscale(pixelData.Span, width, height, bitsAllocated, samplesPerPixel);

        // Run OCR
        var regions = new List<PhiRegion>();

        try
        {
            using var pix = Pix.LoadFromMemory(grayscale);
            using var page = _engine.Process(pix);

            // Get word-level bounding boxes
            using var iter = page.GetIterator();
            iter.Begin();

            do
            {
                if (iter.TryGetBoundingBox(PageIteratorLevel.Word, out var bounds))
                {
                    var confidence = iter.GetConfidence(PageIteratorLevel.Word) / 100f;

                    if (confidence >= _confidenceThreshold)
                    {
                        regions.Add(new PhiRegion(
                            bounds.X1,
                            bounds.Y1,
                            bounds.Width,
                            bounds.Height,
                            PhiRegionType.Text,
                            confidence));
                    }
                }
            } while (iter.Next(PageIteratorLevel.Word));
        }
        catch
        {
            // OCR failed - fall back to heuristic
            return await _fallback.DetectAsync(
                pixelData, width, height, bitsAllocated, samplesPerPixel, modality, ct);
        }

        // Merge overlapping regions
        regions = MergeOverlappingRegions(regions);

        // Combine with heuristic regions for modality-specific areas
        var heuristicResult = await _fallback.DetectAsync(
            pixelData, width, height, bitsAllocated, samplesPerPixel, modality, ct);

        // Union the regions
        foreach (var hr in heuristicResult.Regions)
        {
            if (!regions.Any(r => RegionsOverlap(r, hr)))
            {
                regions.Add(hr);
            }
        }

        return new PhiDetectionResult(regions, HighRiskModalities.IsHighRisk(modality));
    }

    private static byte[] ConvertToGrayscale(
        ReadOnlySpan<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel)
    {
        var result = new byte[width * height];

        if (bitsAllocated == 8 && samplesPerPixel == 1)
        {
            // Already 8-bit grayscale
            pixelData.Slice(0, Math.Min(pixelData.Length, result.Length)).CopyTo(result);
        }
        else if (bitsAllocated == 16 && samplesPerPixel == 1)
        {
            // 16-bit grayscale - scale to 8-bit
            for (int i = 0; i < result.Length && i * 2 + 1 < pixelData.Length; i++)
            {
                var value16 = (ushort)(pixelData[i * 2] | (pixelData[i * 2 + 1] << 8));
                result[i] = (byte)(value16 >> 8); // Take high byte
            }
        }
        else if (bitsAllocated == 8 && samplesPerPixel == 3)
        {
            // RGB - convert to grayscale
            for (int i = 0; i < result.Length && i * 3 + 2 < pixelData.Length; i++)
            {
                var r = pixelData[i * 3];
                var g = pixelData[i * 3 + 1];
                var b = pixelData[i * 3 + 2];
                result[i] = (byte)((r * 77 + g * 150 + b * 29) >> 8);
            }
        }

        return result;
    }

    private static List<PhiRegion> MergeOverlappingRegions(List<PhiRegion> regions)
    {
        if (regions.Count <= 1)
            return regions;

        var merged = new List<PhiRegion>();
        var used = new bool[regions.Count];

        for (int i = 0; i < regions.Count; i++)
        {
            if (used[i])
                continue;

            var current = regions[i];

            for (int j = i + 1; j < regions.Count; j++)
            {
                if (used[j])
                    continue;

                if (RegionsOverlap(current, regions[j]))
                {
                    // Merge into bounding box
                    var minX = Math.Min(current.X, regions[j].X);
                    var minY = Math.Min(current.Y, regions[j].Y);
                    var maxX = Math.Max(current.X + current.Width, regions[j].X + regions[j].Width);
                    var maxY = Math.Max(current.Y + current.Height, regions[j].Y + regions[j].Height);

                    current = new PhiRegion(
                        minX, minY, maxX - minX, maxY - minY,
                        PhiRegionType.Text,
                        Math.Max(current.Confidence, regions[j].Confidence));

                    used[j] = true;
                }
            }

            merged.Add(current);
        }

        return merged;
    }

    private static bool RegionsOverlap(PhiRegion a, PhiRegion b)
    {
        return a.X < b.X + b.Width &&
               a.X + a.Width > b.X &&
               a.Y < b.Y + b.Height &&
               a.Y + a.Height > b.Y;
    }

    public void Dispose()
    {
        if (_disposed)
            return;

        _disposed = true;
        _engine?.Dispose();
    }
#else
    private readonly HeuristicPhiDetector _fallback = new();

    /// <summary>
    /// Tesseract is not available on this target framework.
    /// </summary>
    public bool IsOcrAvailable => false;

    /// <inheritdoc />
    public ValueTask<PhiDetectionResult> DetectAsync(
        ReadOnlyMemory<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel,
        string? modality,
        CancellationToken ct = default)
    {
        // Fall back to heuristic detection
        return _fallback.DetectAsync(
            pixelData, width, height, bitsAllocated, samplesPerPixel, modality, ct);
    }

    public void Dispose() { }
#endif
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj --framework net9.0`
`dotnet build src/SharpDicom/SharpDicom.csproj --framework netstandard2.0`
  </verify>
  <done>TesseractPhiDetector implements IBurnedInPhiDetector with graceful fallback</done>
</task>

<task type="auto">
  <name>Task 3: Create TesseractPhiDetector tests</name>
  <files>tests/SharpDicom.Tests/Deidentification/TesseractPhiDetectorTests.cs</files>
  <action>
Create tests that work with or without Tesseract installed:
```csharp
namespace SharpDicom.Tests.Deidentification;

[TestFixture]
public class TesseractPhiDetectorTests
{
    [Test]
    public void Constructor_WhenTesseractNotAvailable_FallsBackToHeuristic()
    {
        // Arrange & Act - use invalid path to force fallback
        using var detector = new TesseractPhiDetector("/nonexistent/path");

        // Assert - should not throw, just use fallback
        Assert.That(detector.IsOcrAvailable, Is.False);
    }

    [Test]
    public async Task DetectAsync_WhenTesseractNotAvailable_UsesHeuristicDetection()
    {
        // Arrange
        using var detector = new TesseractPhiDetector("/nonexistent/path");

        // Create simple 100x100 8-bit grayscale image
        var width = 100;
        var height = 100;
        var pixelData = new byte[width * height];

        // Act
        var result = await detector.DetectAsync(
            pixelData, width, height, 8, 1, "US", default);

        // Assert - should still get heuristic regions for ultrasound
        Assert.That(result.IsHighRisk, Is.True);
    }

    [Test]
    public async Task DetectAsync_WithRgbImage_HandlesConversion()
    {
        // Arrange
        using var detector = new TesseractPhiDetector("/nonexistent/path");

        // Create simple 100x100 RGB image
        var width = 100;
        var height = 100;
        var pixelData = new byte[width * height * 3]; // RGB

        // Act & Assert - should not throw
        var result = await detector.DetectAsync(
            pixelData, width, height, 8, 3, "SC", default);

        Assert.That(result, Is.Not.Null);
    }

    [Test]
    public async Task DetectAsync_With16BitImage_HandlesConversion()
    {
        // Arrange
        using var detector = new TesseractPhiDetector("/nonexistent/path");

        // Create simple 100x100 16-bit grayscale image
        var width = 100;
        var height = 100;
        var pixelData = new byte[width * height * 2]; // 16-bit

        // Act & Assert - should not throw
        var result = await detector.DetectAsync(
            pixelData, width, height, 16, 1, "CT", default);

        Assert.That(result, Is.Not.Null);
    }

    [Test]
    [Category("Integration")]
    [Explicit("Requires Tesseract tessdata installed")]
    public async Task DetectAsync_WithRealTesseract_DetectsText()
    {
        // This test only runs when Tesseract is properly installed
        var tessdataPath = Environment.GetEnvironmentVariable("TESSDATA_PREFIX");
        if (string.IsNullOrEmpty(tessdataPath) || !Directory.Exists(tessdataPath))
        {
            Assert.Ignore("TESSDATA_PREFIX not set or tessdata not found");
        }

        // Arrange
        using var detector = new TesseractPhiDetector(tessdataPath);

        if (!detector.IsOcrAvailable)
        {
            Assert.Ignore("Tesseract engine not available");
        }

        // Create a simple image with text-like patterns
        // (Real testing would use actual DICOM ultrasound images)
        var width = 512;
        var height = 512;
        var pixelData = new byte[width * height];

        // Act
        var result = await detector.DetectAsync(
            pixelData, width, height, 8, 1, "US", default);

        // Assert
        Assert.That(result, Is.Not.Null);
        Assert.That(result.IsHighRisk, Is.True); // US is high risk
    }

    [Test]
    public void Dispose_MultipleCalls_DoesNotThrow()
    {
        // Arrange
        var detector = new TesseractPhiDetector("/nonexistent/path");

        // Act & Assert - should not throw
        detector.Dispose();
        detector.Dispose();
    }
}
```
  </action>
  <verify>
`dotnet build tests/SharpDicom.Tests/SharpDicom.Tests.csproj`
`dotnet test tests/SharpDicom.Tests/SharpDicom.Tests.csproj --filter "FullyQualifiedName~TesseractPhiDetectorTests" --no-build`
  </verify>
  <done>TesseractPhiDetector tests pass (with fallback when Tesseract not installed)</done>
</task>

</tasks>

<verification>
1. `dotnet build` - entire solution builds (both net9.0 and netstandard2.0)
2. `dotnet test` - all tests pass (fallback behavior when Tesseract not installed)
3. Verify graceful degradation: TesseractPhiDetector works without Tesseract installed
</verification>

<success_criteria>
- TesseractOCR package added with conditional compilation
- TesseractPhiDetector implements IBurnedInPhiDetector interface
- Detector falls back to HeuristicPhiDetector when Tesseract unavailable
- Detector handles 8-bit, 16-bit, grayscale, and RGB images
- Tests pass regardless of Tesseract installation status
- Integration test available for environments with Tesseract
</success_criteria>

<output>
After completion, create `.planning/phases/14-de-identification/14-07-SUMMARY.md`
</output>
