---
phase: 14-de-identification
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/SharpDicom/Deidentification/DicomDeidentifier.cs
  - src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs
  - src/SharpDicom/Deidentification/DateShifter.cs
autonomous: true

must_haves:
  truths:
    - "Deidentifier applies actions per profile for each tag"
    - "UID elements are remapped through context"
    - "Date elements are shifted per strategy"
    - "PatientAge is recalculated from shifted dates"
    - "Fluent API builds configured deidentifier"
  artifacts:
    - path: "src/SharpDicom/Deidentification/DicomDeidentifier.cs"
      provides: "Main de-identification engine"
      exports: ["DicomDeidentifier", "ApplyAsync"]
    - path: "src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs"
      provides: "Fluent configuration builder"
      exports: ["DicomDeidentifierBuilder", "Create", "Build"]
    - path: "src/SharpDicom/Deidentification/DateShifter.cs"
      provides: "Date/time VR-aware shifting"
      exports: ["DateShifter"]
  key_links:
    - from: "DicomDeidentifier.cs"
      to: "DeidentificationActionTable.Generated.cs"
      via: "GetAction lookup"
      pattern: "DeidentificationActionTable\\.GetAction"
    - from: "DicomDeidentifier.cs"
      to: "DeidentificationContext.cs"
      via: "UID remapping"
      pattern: "_context\\.RemapUID"
    - from: "DateShifter.cs"
      to: "DicomVR"
      via: "VR-specific date handling"
      pattern: "DicomVR\\.DA|DicomVR\\.TM|DicomVR\\.DT"
---

<objective>
Implement the DicomDeidentifier class with fluent builder API that applies de-identification actions to datasets using the generated action table, context-aware UID remapping, and VR-aware date shifting.

Purpose: This is the main API users interact with - it orchestrates action lookup, UID remapping, date shifting, and sequence traversal.

Output: Complete DicomDeidentifier with fluent API and date shifting logic
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-de-identification/14-CONTEXT.md
@.planning/phases/14-de-identification/14-RESEARCH.md
@src/SharpDicom/Data/DicomDataset.cs
@src/SharpDicom/Data/DicomElement.cs
@src/SharpDicom/Data/DicomSequence.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateShifter for VR-aware date handling</name>
  <files>src/SharpDicom/Deidentification/DateShifter.cs</files>
  <action>
Create DateShifter class for handling DA, TM, DT VRs:
```csharp
namespace SharpDicom.Deidentification;

/// <summary>
/// Handles date/time shifting with VR-aware logic per PS3.15.
/// </summary>
public static class DateShifter
{
    /// <summary>
    /// Shifts a date element by the given offset.
    /// </summary>
    public static IDicomElement Shift(IDicomElement element, TimeSpan offset, bool zeroTime)
    {
        if (element is not DicomStringElement se)
            return element;

        return element.VR.Code switch
        {
            "DA" => ShiftDate(element.Tag, se, offset),
            "TM" => zeroTime ? ZeroTime(element.Tag) : element,
            "DT" => ShiftDateTime(element.Tag, se, offset, zeroTime),
            _ => element
        };
    }

    private static IDicomElement ShiftDate(DicomTag tag, DicomStringElement element, TimeSpan offset)
    {
        var value = element.GetString();
        if (string.IsNullOrEmpty(value) || value.Length < 8)
            return element;

        // Parse YYYYMMDD format
        if (!int.TryParse(value.Substring(0, 4), out var year) ||
            !int.TryParse(value.Substring(4, 2), out var month) ||
            !int.TryParse(value.Substring(6, 2), out var day))
            return element;

        try
        {
            var date = new DateOnly(year, month, day);
            var shifted = date.AddDays((int)offset.TotalDays);
            return DicomElement.Create(tag, DicomVR.DA,
                $"{shifted.Year:D4}{shifted.Month:D2}{shifted.Day:D2}");
        }
        catch
        {
            return element; // Invalid date - keep original
        }
    }

    private static IDicomElement ShiftDateTime(DicomTag tag, DicomStringElement element,
        TimeSpan offset, bool zeroTime)
    {
        var value = element.GetString();
        if (string.IsNullOrEmpty(value) || value.Length < 8)
            return element;

        // Parse YYYYMMDD portion
        if (!int.TryParse(value.Substring(0, 4), out var year) ||
            !int.TryParse(value.Substring(4, 2), out var month) ||
            !int.TryParse(value.Substring(6, 2), out var day))
            return element;

        try
        {
            var date = new DateOnly(year, month, day);
            var shifted = date.AddDays((int)offset.TotalDays);

            if (zeroTime)
            {
                // YYYYMMDD000000.000000+0000 format with zeroed time
                return DicomElement.Create(tag, DicomVR.DT,
                    $"{shifted.Year:D4}{shifted.Month:D2}{shifted.Day:D2}000000");
            }
            else
            {
                // Keep original time portion if present
                var time = value.Length > 8 ? value.Substring(8) : "";
                return DicomElement.Create(tag, DicomVR.DT,
                    $"{shifted.Year:D4}{shifted.Month:D2}{shifted.Day:D2}{time}");
            }
        }
        catch
        {
            return element;
        }
    }

    private static IDicomElement ZeroTime(DicomTag tag)
    {
        return DicomElement.Create(tag, DicomVR.TM, "000000");
    }

    /// <summary>
    /// Recalculates PatientAge (AS VR) from birth date and study date.
    /// </summary>
    public static string? CalculateAge(DateOnly? birthDate, DateOnly? studyDate)
    {
        if (birthDate == null || studyDate == null)
            return null;

        var years = studyDate.Value.Year - birthDate.Value.Year;
        if (studyDate.Value.DayOfYear < birthDate.Value.DayOfYear)
            years--;

        if (years < 0)
            return "000Y";

        // AS VR format: nnnD, nnnW, nnnM, or nnnY
        return $"{Math.Min(years, 999):D3}Y";
    }

    /// <summary>
    /// Parses a DA (Date) value to DateOnly.
    /// </summary>
    public static DateOnly? ParseDate(string? value)
    {
        if (string.IsNullOrEmpty(value) || value.Length < 8)
            return null;

        if (int.TryParse(value.Substring(0, 4), out var year) &&
            int.TryParse(value.Substring(4, 2), out var month) &&
            int.TryParse(value.Substring(6, 2), out var day))
        {
            try { return new DateOnly(year, month, day); }
            catch { return null; }
        }
        return null;
    }
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>DateShifter handles DA, TM, DT VRs with offset and time zeroing</done>
</task>

<task type="auto">
  <name>Task 2: Create DicomDeidentifier core engine</name>
  <files>src/SharpDicom/Deidentification/DicomDeidentifier.cs</files>
  <action>
Create main de-identification engine:
```csharp
namespace SharpDicom.Deidentification;

/// <summary>
/// Applies DICOM de-identification according to PS3.15 profiles.
/// </summary>
public sealed class DicomDeidentifier
{
    private readonly DeidentificationOptions _options;
    private readonly DeidentificationContext _context;

    public DicomDeidentifier(DeidentificationOptions options, DeidentificationContext? context = null)
    {
        _options = options ?? throw new ArgumentNullException(nameof(options));
        _context = context ?? new DeidentificationContext(options);
    }

    /// <summary>Gets the context for UID/date mapping access.</summary>
    public DeidentificationContext Context => _context;

    /// <summary>
    /// Applies de-identification to the dataset in-place.
    /// </summary>
    public async ValueTask ApplyAsync(DicomDataset dataset, CancellationToken ct = default)
    {
        // Get patient/study identifiers for date offset lookup
        var patientId = GetStringValue(dataset, DicomTag.PatientID) ?? "";
        var studyUid = GetUidValue(dataset, DicomTag.StudyInstanceUID);

        var dateOffset = _options.DateShiftStrategy switch
        {
            DateShiftStrategy.PerPatient => _context.GetDateOffset(patientId),
            DateShiftStrategy.PerStudy when studyUid != default =>
                _context.GetStudyDateOffset(studyUid),
            _ => TimeSpan.Zero
        };

        // Track dates for PatientAge recalculation
        DateOnly? birthDate = null;
        DateOnly? studyDate = null;

        // Collect elements to modify (can't modify during enumeration)
        var modifications = new List<(DicomTag Tag, IDicomElement? NewValue)>();

        foreach (var element in dataset)
        {
            ct.ThrowIfCancellationRequested();

            // Apply custom rules first
            if (TryApplyCustomRule(element, out var customResult))
            {
                modifications.Add((element.Tag, customResult));
                continue;
            }

            // Get standard action
            var action = DeidentificationActionTable.GetAction(element.Tag, _options.Profile);

            // Apply action
            var newValue = ApplyAction(element, action, dateOffset);
            if (!ReferenceEquals(newValue, element))
            {
                modifications.Add((element.Tag, newValue));
            }

            // Track birth/study dates for age recalculation
            if (element.Tag == DicomTag.PatientBirthDate && element is DicomStringElement birthEl)
            {
                var shifted = newValue as DicomStringElement ?? birthEl;
                birthDate = DateShifter.ParseDate(shifted.GetString());
            }
            if (element.Tag == DicomTag.StudyDate && element is DicomStringElement studyEl)
            {
                var shifted = newValue as DicomStringElement ?? studyEl;
                studyDate = DateShifter.ParseDate(shifted.GetString());
            }
        }

        // Apply modifications
        foreach (var (tag, newValue) in modifications)
        {
            if (newValue == null)
                dataset.Remove(tag);
            else
                dataset.Add(newValue);
        }

        // Recalculate PatientAge if enabled
        if (_options.RecalculatePatientAge && birthDate != null && studyDate != null)
        {
            var age = DateShifter.CalculateAge(birthDate, studyDate);
            if (age != null)
            {
                dataset.Add(DicomElement.Create(DicomTag.PatientAge, DicomVR.AS, age));
            }
        }

        // Remove private tags if configured
        if (_options.RemovePrivateTags)
        {
            dataset.StripPrivateTags(_options.SafePrivateCreators != null
                ? c => _options.SafePrivateCreators.Contains(c)
                : null);
        }
    }

    private IDicomElement? ApplyAction(IDicomElement element, DeidentificationAction action,
        TimeSpan dateOffset)
    {
        return action switch
        {
            DeidentificationAction.Remove => null,
            DeidentificationAction.Zero => CreateZeroLength(element),
            DeidentificationAction.Dummy => CreateDummy(element),
            DeidentificationAction.UidRemap => RemapUid(element),
            DeidentificationAction.Clean => CleanElement(element, dateOffset),
            DeidentificationAction.Keep => ProcessKeep(element, dateOffset),
            _ => element
        };
    }

    private IDicomElement CreateZeroLength(IDicomElement element)
    {
        return DicomElement.Create(element.Tag, element.VR, ReadOnlySpan<byte>.Empty);
    }

    private IDicomElement CreateDummy(IDicomElement element)
    {
        // Type-1 safe dummy values per VR
        return element.VR.Code switch
        {
            "PN" => DicomElement.Create(element.Tag, element.VR, "ANONYMOUS"),
            "LO" or "SH" => DicomElement.Create(element.Tag, element.VR, "REMOVED"),
            "DA" => DicomElement.Create(element.Tag, element.VR, "19000101"),
            "TM" => DicomElement.Create(element.Tag, element.VR, "000000"),
            "DT" => DicomElement.Create(element.Tag, element.VR, "19000101000000"),
            "UI" => DicomElement.Create(element.Tag, element.VR, DicomUID.Generate().ToString()),
            "AS" => DicomElement.Create(element.Tag, element.VR, "000Y"),
            "CS" => DicomElement.Create(element.Tag, element.VR, "UNKNOWN"),
            "IS" or "DS" => DicomElement.Create(element.Tag, element.VR, "0"),
            _ => element
        };
    }

    private IDicomElement? RemapUid(IDicomElement element)
    {
        if (element is not DicomStringElement se)
            return element;

        var original = se.GetString();
        if (string.IsNullOrEmpty(original))
            return element;

        var originalUid = new DicomUID(original);
        var newUid = _context.RemapUID(originalUid);
        return DicomElement.Create(element.Tag, element.VR, newUid.ToString());
    }

    private IDicomElement CleanElement(IDicomElement element, TimeSpan dateOffset)
    {
        // Clean action varies by VR - dates get shifted, text gets dummy
        if (element.VR.Code is "DA" or "TM" or "DT")
        {
            return DateShifter.Shift(element, dateOffset, _options.ZeroTimeComponents);
        }
        return CreateDummy(element);
    }

    private IDicomElement ProcessKeep(IDicomElement element, TimeSpan dateOffset)
    {
        // Keep the element but process nested sequences
        if (element is DicomSequence seq)
        {
            foreach (var item in seq.Items)
            {
                ApplyAsync(item, default).AsTask().GetAwaiter().GetResult();
            }
        }
        return element;
    }

    private bool TryApplyCustomRule(IDicomElement element, out IDicomElement? result)
    {
        result = null;
        if (_options.CustomRules == null)
            return false;

        foreach (var rule in _options.CustomRules)
        {
            if (rule.AppliesTo(element.Tag))
            {
                result = rule.Transform(element, _context);
                return true;
            }
        }
        return false;
    }

    private static string? GetStringValue(DicomDataset dataset, DicomTag tag)
    {
        return dataset[tag] is DicomStringElement se ? se.GetString() : null;
    }

    private static DicomUID GetUidValue(DicomDataset dataset, DicomTag tag)
    {
        var str = GetStringValue(dataset, tag);
        return string.IsNullOrEmpty(str) ? default : new DicomUID(str);
    }
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>DicomDeidentifier applies actions, remaps UIDs, shifts dates</done>
</task>

<task type="auto">
  <name>Task 3: Create fluent builder API</name>
  <files>src/SharpDicom/Deidentification/DicomDeidentifierBuilder.cs</files>
  <action>
Create fluent builder:
```csharp
namespace SharpDicom.Deidentification;

/// <summary>
/// Fluent builder for configuring and creating DicomDeidentifier instances.
/// </summary>
public sealed class DicomDeidentifierBuilder
{
    private DeidentificationProfile _profile = DeidentificationProfile.Basic;
    private DateShiftStrategy _dateStrategy = DateShiftStrategy.PerPatient;
    private (int Min, int Max) _dateRange = (-365, 365);
    private bool _zeroTime = true;
    private bool _recalcAge = true;
    private string _uidPrefix = "2.25";
    private bool _removePrivate = true;
    private List<string>? _safePrivate;
    private List<IDeidentificationRule>? _customRules;
    private PixelCleaningOptions _pixelOptions = new();
    private DeidentificationContext? _context;

    private DicomDeidentifierBuilder() { }

    /// <summary>Creates a new builder instance.</summary>
    public static DicomDeidentifierBuilder Create() => new();

    /// <summary>Sets the base profile (default: Basic).</summary>
    public DicomDeidentifierBuilder WithProfile(DeidentificationProfile profile)
    {
        _profile = profile;
        return this;
    }

    /// <summary>Adds an option profile to the base profile.</summary>
    public DicomDeidentifierBuilder WithOption(DeidentificationProfile option)
    {
        _profile |= option;
        return this;
    }

    /// <summary>Configures date shifting range in days (default: -365 to +365).</summary>
    public DicomDeidentifierBuilder WithDateShift(int minDays, int maxDays)
    {
        _dateRange = (minDays, maxDays);
        return this;
    }

    /// <summary>Configures date shift strategy (default: PerPatient).</summary>
    public DicomDeidentifierBuilder WithDateStrategy(DateShiftStrategy strategy)
    {
        _dateStrategy = strategy;
        return this;
    }

    /// <summary>Controls whether time components are zeroed (default: true).</summary>
    public DicomDeidentifierBuilder WithZeroTime(bool zero = true)
    {
        _zeroTime = zero;
        return this;
    }

    /// <summary>Controls PatientAge recalculation (default: true).</summary>
    public DicomDeidentifierBuilder WithRecalculateAge(bool recalc = true)
    {
        _recalcAge = recalc;
        return this;
    }

    /// <summary>Sets UID prefix for generated UIDs (default: 2.25).</summary>
    public DicomDeidentifierBuilder WithUidPrefix(string prefix)
    {
        _uidPrefix = prefix ?? throw new ArgumentNullException(nameof(prefix));
        return this;
    }

    /// <summary>Controls private tag removal (default: true).</summary>
    public DicomDeidentifierBuilder WithRemovePrivateTags(bool remove = true)
    {
        _removePrivate = remove;
        return this;
    }

    /// <summary>Adds a safe private creator to whitelist.</summary>
    public DicomDeidentifierBuilder WithSafePrivateCreator(string creator)
    {
        _safePrivate ??= new List<string>();
        _safePrivate.Add(creator);
        return this;
    }

    /// <summary>Adds a custom rule.</summary>
    public DicomDeidentifierBuilder WithCustomRule(IDeidentificationRule rule)
    {
        _customRules ??= new List<IDeidentificationRule>();
        _customRules.Add(rule);
        return this;
    }

    /// <summary>Configures pixel cleaning options.</summary>
    public DicomDeidentifierBuilder WithPixelCleaning(Action<PixelCleaningOptions> configure)
    {
        var options = new PixelCleaningOptions();
        configure(options);
        _pixelOptions = options;
        return this;
    }

    /// <summary>Uses an existing context for UID/date mapping.</summary>
    public DicomDeidentifierBuilder WithContext(DeidentificationContext context)
    {
        _context = context;
        return this;
    }

    /// <summary>Builds the configured deidentifier.</summary>
    public DicomDeidentifier Build()
    {
        var options = new DeidentificationOptions
        {
            Profile = _profile,
            DateShiftStrategy = _dateStrategy,
            DateShiftRange = _dateRange,
            ZeroTimeComponents = _zeroTime,
            RecalculatePatientAge = _recalcAge,
            UidPrefix = _uidPrefix,
            RemovePrivateTags = _removePrivate,
            SafePrivateCreators = _safePrivate?.AsReadOnly(),
            CustomRules = _customRules?.AsReadOnly(),
            PixelCleaning = _pixelOptions
        };

        return new DicomDeidentifier(options, _context);
    }
}

// Extension for DicomDeidentifier to provide Create() entry point
public partial class DicomDeidentifier
{
    /// <summary>Creates a new fluent builder for de-identification configuration.</summary>
    public static DicomDeidentifierBuilder Create() => DicomDeidentifierBuilder.Create();
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>Fluent API provides discoverable configuration: DicomDeidentifier.Create().WithProfile(...).Build()</done>
</task>

</tasks>

<verification>
1. `dotnet build` - entire solution builds
2. `dotnet test` - existing tests pass
3. Verify fluent API compiles: Check that builder methods chain correctly
</verification>

<success_criteria>
- DateShifter handles DA, TM, DT VRs with offset and time zeroing
- DicomDeidentifier applies profile actions, remaps UIDs, shifts dates
- DicomDeidentifier processes nested sequences recursively
- PatientAge recalculated from shifted birth/study dates
- Fluent builder enables discoverable configuration
- Both builder and options-based construction work
</success_criteria>

<output>
After completion, create `.planning/phases/14-de-identification/14-03-SUMMARY.md`
</output>
