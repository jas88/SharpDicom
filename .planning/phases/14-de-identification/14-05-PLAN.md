---
phase: 14-de-identification
plan: 05
type: execute
wave: 3
depends_on: ["14-03", "14-04"]
files_modified:
  - src/SharpDicom/Deidentification/DicomDeidentifier.cs
  - src/SharpDicom/Deidentification/PixelCleaner/PixelDataCleaner.cs
  - src/SharpDicom/Deidentification/StudyDeidentifier.cs
autonomous: true

must_haves:
  truths:
    - "Deidentifier warns for high-risk modalities"
    - "Deidentifier processes overlay planes when configured"
    - "Pixel regions can be cleaned/replaced"
    - "StudyDeidentifier processes multiple files with shared context"
  artifacts:
    - path: "src/SharpDicom/Deidentification/PixelCleaner/PixelDataCleaner.cs"
      provides: "Pixel region cleaning"
      exports: ["PixelDataCleaner"]
    - path: "src/SharpDicom/Deidentification/StudyDeidentifier.cs"
      provides: "Multi-file study processing"
      exports: ["StudyDeidentifier"]
  key_links:
    - from: "DicomDeidentifier.cs"
      to: "PixelDataCleaner.cs"
      via: "pixel cleaning"
      pattern: "PixelDataCleaner\\.Clean"
    - from: "StudyDeidentifier.cs"
      to: "DicomDeidentifier.cs"
      via: "per-file processing"
      pattern: "_deidentifier\\.ApplyAsync"
---

<objective>
Integrate pixel cleaning into DicomDeidentifier, add high-risk modality warnings, and create StudyDeidentifier for batch processing multiple files with shared context.

Purpose: Complete the de-identification pipeline with pixel data handling and enable efficient multi-file study processing with consistent UID/date mappings.

Output: Full-featured de-identification system ready for production use
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-de-identification/14-CONTEXT.md
@.planning/phases/14-de-identification/14-03-SUMMARY.md
@.planning/phases/14-de-identification/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PixelDataCleaner for region replacement</name>
  <files>src/SharpDicom/Deidentification/PixelCleaner/PixelDataCleaner.cs</files>
  <action>
Create pixel region cleaner:
```csharp
namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// Cleans (blacks out/replaces) detected PHI regions in pixel data.
/// </summary>
public static class PixelDataCleaner
{
    /// <summary>
    /// Cleans detected regions in pixel data by replacing with specified value.
    /// </summary>
    /// <param name="pixelData">Pixel data buffer (modified in-place).</param>
    /// <param name="width">Image width.</param>
    /// <param name="height">Image height.</param>
    /// <param name="bitsAllocated">Bits per sample (8, 16).</param>
    /// <param name="samplesPerPixel">1 for grayscale, 3 for RGB.</param>
    /// <param name="regions">Regions to clean.</param>
    /// <param name="replacement">Replacement value type.</param>
    public static void Clean(
        Span<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel,
        IReadOnlyList<PhiRegion> regions,
        PixelReplacementValue replacement)
    {
        if (regions.Count == 0)
            return;

        var bytesPerSample = bitsAllocated / 8;
        var bytesPerPixel = bytesPerSample * samplesPerPixel;
        var rowStride = width * bytesPerPixel;

        foreach (var region in regions)
        {
            CleanRegion(pixelData, width, height, bytesPerPixel, rowStride,
                region, replacement, bitsAllocated, samplesPerPixel);
        }
    }

    private static void CleanRegion(
        Span<byte> pixelData,
        int width,
        int height,
        int bytesPerPixel,
        int rowStride,
        PhiRegion region,
        PixelReplacementValue replacement,
        int bitsAllocated,
        int samplesPerPixel)
    {
        // Clamp region to image bounds
        var x1 = Math.Max(0, region.X);
        var y1 = Math.Max(0, region.Y);
        var x2 = Math.Min(width, region.X + region.Width);
        var y2 = Math.Min(height, region.Y + region.Height);

        if (x2 <= x1 || y2 <= y1)
            return;

        // Calculate replacement value
        byte replacementByte;
        ushort replacementShort;

        if (replacement == PixelReplacementValue.AverageOfRegion)
        {
            // Calculate average of region
            (replacementByte, replacementShort) = CalculateRegionAverage(
                pixelData, rowStride, bytesPerPixel, x1, y1, x2, y2, bitsAllocated);
        }
        else
        {
            replacementByte = replacement == PixelReplacementValue.Black ? (byte)0 : (byte)255;
            replacementShort = replacement == PixelReplacementValue.Black ? (ushort)0 : (ushort)65535;
        }

        // Fill region
        for (int y = y1; y < y2; y++)
        {
            var rowStart = y * rowStride + x1 * bytesPerPixel;
            var rowEnd = y * rowStride + x2 * bytesPerPixel;

            if (bitsAllocated == 8)
            {
                pixelData.Slice(rowStart, rowEnd - rowStart).Fill(replacementByte);
            }
            else if (bitsAllocated == 16)
            {
                var row = pixelData.Slice(rowStart, rowEnd - rowStart);
                for (int i = 0; i < row.Length; i += 2)
                {
                    if (i + 1 < row.Length)
                    {
                        row[i] = (byte)(replacementShort & 0xFF);
                        row[i + 1] = (byte)((replacementShort >> 8) & 0xFF);
                    }
                }
            }
        }
    }

    private static (byte Byte, ushort Short) CalculateRegionAverage(
        ReadOnlySpan<byte> pixelData,
        int rowStride,
        int bytesPerPixel,
        int x1, int y1, int x2, int y2,
        int bitsAllocated)
    {
        long sum = 0;
        int count = 0;

        for (int y = y1; y < y2; y++)
        {
            for (int x = x1; x < x2; x++)
            {
                var offset = y * rowStride + x * bytesPerPixel;
                if (offset + bytesPerPixel <= pixelData.Length)
                {
                    if (bitsAllocated == 8)
                    {
                        sum += pixelData[offset];
                    }
                    else if (bitsAllocated == 16 && offset + 1 < pixelData.Length)
                    {
                        sum += pixelData[offset] | (pixelData[offset + 1] << 8);
                    }
                    count++;
                }
            }
        }

        if (count == 0)
            return (0, 0);

        var avg = sum / count;
        return ((byte)Math.Min(255, avg), (ushort)Math.Min(65535, avg));
    }

    /// <summary>
    /// Cleans pixel data using the specified detector and options.
    /// </summary>
    public static async ValueTask CleanAsync(
        DicomDataset dataset,
        IBurnedInPhiDetector detector,
        PixelCleaningOptions options,
        CancellationToken ct = default)
    {
        // Get pixel data element
        var pixelDataElement = dataset[DicomTag.PixelData];
        if (pixelDataElement == null)
            return;

        // Get image parameters
        var width = GetUShort(dataset, DicomTag.Columns);
        var height = GetUShort(dataset, DicomTag.Rows);
        var bitsAllocated = GetUShort(dataset, DicomTag.BitsAllocated);
        var samplesPerPixel = GetUShort(dataset, DicomTag.SamplesPerPixel);
        var modality = GetString(dataset, DicomTag.Modality);

        if (width == 0 || height == 0 || bitsAllocated == 0)
            return;

        if (samplesPerPixel == 0)
            samplesPerPixel = 1;

        // Get pixel data bytes
        if (pixelDataElement is not DicomOtherElement oe)
            return;

        var pixelBytes = oe.GetBytes();
        if (pixelBytes.Length == 0)
            return;

        // Detect PHI regions
        var result = await detector.DetectAsync(
            pixelBytes, width, height, bitsAllocated, samplesPerPixel, modality, ct);

        if (result.Regions.Count == 0)
            return;

        // Clean regions (need mutable span)
        var mutableBytes = pixelBytes.ToArray();
        Clean(mutableBytes, width, height, bitsAllocated, samplesPerPixel,
            result.Regions, options.ReplacementValue);

        // Update pixel data
        dataset.Add(DicomElement.Create(DicomTag.PixelData, pixelDataElement.VR, mutableBytes));
    }

    private static ushort GetUShort(DicomDataset ds, DicomTag tag)
    {
        if (ds[tag] is DicomNumericElement ne)
            return ne.GetUInt16();
        return 0;
    }

    private static string? GetString(DicomDataset ds, DicomTag tag)
    {
        if (ds[tag] is DicomStringElement se)
            return se.GetString();
        return null;
    }
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>PixelDataCleaner replaces detected regions in pixel data</done>
</task>

<task type="auto">
  <name>Task 2: Integrate pixel cleaning and warnings into DicomDeidentifier</name>
  <files>src/SharpDicom/Deidentification/DicomDeidentifier.cs</files>
  <action>
Update DicomDeidentifier to add:

1. High-risk modality warning event:
```csharp
public event Action<string, DicomDataset>? HighRiskModalityDetected;
```

2. Add pixel cleaning to ApplyAsync after attribute processing:
```csharp
// Check for high-risk modality and warn
var modality = GetStringValue(dataset, DicomTag.Modality);
if (_options.PixelCleaning.WarnHighRiskModalities &&
    HighRiskModalities.IsHighRisk(modality))
{
    HighRiskModalityDetected?.Invoke(modality!, dataset);
}

// Check BurnedInAnnotation tag
var burnedIn = GetStringValue(dataset, new DicomTag(0x0028, 0x0301));
var hasBurnedIn = string.Equals(burnedIn, "YES", StringComparison.OrdinalIgnoreCase);

// Process pixel data if configured
if (_options.PixelCleaning.Enabled || hasBurnedIn)
{
    var detector = new HeuristicPhiDetector();
    await PixelDataCleaner.CleanAsync(dataset, detector, _options.PixelCleaning, ct);
}

// Process overlay planes if configured
if (_options.PixelCleaning.ProcessOverlayPlanes &&
    OverlayPlaneProcessor.HasOverlayPlanes(dataset))
{
    OverlayPlaneProcessor.RemoveAllOverlays(dataset);
}
```

3. Add method to use custom detector:
```csharp
public async ValueTask ApplyAsync(
    DicomDataset dataset,
    IBurnedInPhiDetector? customDetector,
    CancellationToken ct = default)
{
    // ... existing attribute processing ...

    // Process pixel data with custom or default detector
    if (_options.PixelCleaning.Enabled)
    {
        var detector = customDetector ?? new HeuristicPhiDetector();
        await PixelDataCleaner.CleanAsync(dataset, detector, _options.PixelCleaning, ct);
    }
    // ... rest of processing ...
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>DicomDeidentifier integrates pixel cleaning, warnings, overlay processing</done>
</task>

<task type="auto">
  <name>Task 3: Create StudyDeidentifier for batch processing</name>
  <files>src/SharpDicom/Deidentification/StudyDeidentifier.cs</files>
  <action>
Create multi-file study processor:
```csharp
namespace SharpDicom.Deidentification;

/// <summary>
/// Processes multiple DICOM files as a study with shared de-identification context.
/// Ensures consistent UID remapping and date shifting across all files in the study.
/// </summary>
public sealed class StudyDeidentifier : IAsyncDisposable
{
    private readonly DicomDeidentifier _deidentifier;
    private readonly DeidentificationOptions _options;
    private int _filesProcessed;
    private int _warningsRaised;

    /// <summary>
    /// Creates a new study de-identifier with the given options.
    /// </summary>
    public StudyDeidentifier(DeidentificationOptions options)
    {
        _options = options ?? throw new ArgumentNullException(nameof(options));
        _deidentifier = new DicomDeidentifier(options);
        _deidentifier.HighRiskModalityDetected += OnHighRiskModality;
    }

    /// <summary>
    /// Creates a study de-identifier using an existing context for resuming processing.
    /// </summary>
    public StudyDeidentifier(DeidentificationOptions options, DeidentificationContext context)
    {
        _options = options ?? throw new ArgumentNullException(nameof(options));
        _deidentifier = new DicomDeidentifier(options, context);
        _deidentifier.HighRiskModalityDetected += OnHighRiskModality;
    }

    /// <summary>Gets the shared de-identification context.</summary>
    public DeidentificationContext Context => _deidentifier.Context;

    /// <summary>Gets the number of files processed.</summary>
    public int FilesProcessed => _filesProcessed;

    /// <summary>Gets the number of high-risk modality warnings raised.</summary>
    public int WarningsRaised => _warningsRaised;

    /// <summary>Raised when a high-risk modality is detected.</summary>
    public event Action<string, string?>? Warning;

    /// <summary>
    /// Processes a single DICOM file.
    /// </summary>
    public async ValueTask ProcessFileAsync(
        string inputPath,
        string outputPath,
        CancellationToken ct = default)
    {
        var file = await DicomFile.OpenAsync(inputPath, ct: ct);
        await _deidentifier.ApplyAsync(file.Dataset, ct);
        await file.SaveAsync(outputPath, ct: ct);
        Interlocked.Increment(ref _filesProcessed);
    }

    /// <summary>
    /// Processes a directory of DICOM files.
    /// </summary>
    public async IAsyncEnumerable<(string Input, string Output, bool Success, Exception? Error)>
        ProcessDirectoryAsync(
            string inputDir,
            string outputDir,
            string searchPattern = "*.dcm",
            bool recursive = true,
            [EnumeratorCancellation] CancellationToken ct = default)
    {
        Directory.CreateDirectory(outputDir);

        var searchOption = recursive ? SearchOption.AllDirectories : SearchOption.TopDirectoryOnly;
        var files = Directory.EnumerateFiles(inputDir, searchPattern, searchOption);

        foreach (var inputPath in files)
        {
            ct.ThrowIfCancellationRequested();

            // Preserve relative directory structure
            var relativePath = Path.GetRelativePath(inputDir, inputPath);
            var outputPath = Path.Combine(outputDir, relativePath);
            var outputSubDir = Path.GetDirectoryName(outputPath);
            if (!string.IsNullOrEmpty(outputSubDir))
            {
                Directory.CreateDirectory(outputSubDir);
            }

            Exception? error = null;
            var success = false;

            try
            {
                await ProcessFileAsync(inputPath, outputPath, ct);
                success = true;
            }
            catch (Exception ex) when (ex is not OperationCanceledException)
            {
                error = ex;
            }

            yield return (inputPath, outputPath, success, error);
        }
    }

    /// <summary>
    /// Processes files in parallel for better throughput.
    /// </summary>
    public async Task ProcessParallelAsync(
        IEnumerable<(string Input, string Output)> files,
        int maxDegreeOfParallelism = 4,
        IProgress<(int Processed, int Total)>? progress = null,
        CancellationToken ct = default)
    {
        var fileList = files.ToList();
        var total = fileList.Count;
        var processed = 0;

        await Parallel.ForEachAsync(
            fileList,
            new ParallelOptions
            {
                MaxDegreeOfParallelism = maxDegreeOfParallelism,
                CancellationToken = ct
            },
            async (file, token) =>
            {
                await ProcessFileAsync(file.Input, file.Output, token);
                var current = Interlocked.Increment(ref processed);
                progress?.Report((current, total));
            });
    }

    /// <summary>
    /// Saves the context for later resumption.
    /// </summary>
    public async Task SaveContextAsync(string path, CancellationToken ct = default)
    {
        using var stream = File.Create(path);
        await Context.SaveAsync(stream, ct);
    }

    /// <summary>
    /// Loads a previously saved context.
    /// </summary>
    public static async Task<StudyDeidentifier> LoadAsync(
        string contextPath,
        DeidentificationOptions options,
        CancellationToken ct = default)
    {
        using var stream = File.OpenRead(contextPath);
        var context = await DeidentificationContext.LoadAsync(stream, options, ct);
        return new StudyDeidentifier(options, context);
    }

    private void OnHighRiskModality(string modality, DicomDataset dataset)
    {
        Interlocked.Increment(ref _warningsRaised);
        var sopInstance = dataset[DicomTag.SOPInstanceUID] is DicomStringElement se
            ? se.GetString() : null;
        Warning?.Invoke(modality, sopInstance);
    }

    public ValueTask DisposeAsync()
    {
        Context.Dispose();
        return ValueTask.CompletedTask;
    }
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>StudyDeidentifier enables batch processing with shared context</done>
</task>

</tasks>

<verification>
1. `dotnet build` - entire solution builds
2. `dotnet test` - existing tests pass
3. Verify integration: PixelDataCleaner, OverlayPlaneProcessor, StudyDeidentifier all compile
</verification>

<success_criteria>
- PixelDataCleaner replaces detected regions with configurable value
- DicomDeidentifier raises warning for high-risk modalities (US, SC, XA)
- DicomDeidentifier processes overlay planes (60xx groups)
- StudyDeidentifier processes multiple files with shared context
- StudyDeidentifier supports parallel processing for throughput
- Context can be saved/loaded for resumable batch processing
</success_criteria>

<output>
After completion, create `.planning/phases/14-de-identification/14-05-SUMMARY.md`
</output>
