---
phase: 14-de-identification
plan: 04
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/SharpDicom/Deidentification/PixelCleaner/IBurnedInPhiDetector.cs
  - src/SharpDicom/Deidentification/PixelCleaner/HeuristicPhiDetector.cs
  - src/SharpDicom/Deidentification/PixelCleaner/BurnedInPhiRegions.cs
  - src/SharpDicom/Deidentification/PixelCleaner/OverlayPlaneProcessor.cs
autonomous: true

must_haves:
  truths:
    - "High-risk modalities are identified and warned"
    - "Heuristic detector identifies common PHI regions"
    - "Overlay planes (60xx) are processed for PHI"
    - "Detected regions can be blacked/replaced"
  artifacts:
    - path: "src/SharpDicom/Deidentification/PixelCleaner/IBurnedInPhiDetector.cs"
      provides: "Interface for PHI detection"
      exports: ["IBurnedInPhiDetector", "PhiRegion"]
    - path: "src/SharpDicom/Deidentification/PixelCleaner/HeuristicPhiDetector.cs"
      provides: "Region-based PHI detector"
      exports: ["HeuristicPhiDetector"]
    - path: "src/SharpDicom/Deidentification/PixelCleaner/BurnedInPhiRegions.cs"
      provides: "Modality-specific region definitions"
      exports: ["BurnedInPhiRegions"]
    - path: "src/SharpDicom/Deidentification/PixelCleaner/OverlayPlaneProcessor.cs"
      provides: "Overlay plane PHI handling"
      exports: ["OverlayPlaneProcessor"]
  key_links:
    - from: "HeuristicPhiDetector.cs"
      to: "BurnedInPhiRegions.cs"
      via: "modality region lookup"
      pattern: "BurnedInPhiRegions\\.GetRegions"
---

<objective>
Create the burned-in PHI detection infrastructure with heuristic region detection for common modalities and overlay plane processing for 60xx groups.

Purpose: Burned-in text in pixel data is a major PHI leak vector, especially for Ultrasound (100% risk), Secondary Capture, and XA. This plan implements the detection framework; OCR integration is separate.

Output: Pixel cleaning infrastructure with modality-aware heuristic detection
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-de-identification/14-CONTEXT.md
@.planning/phases/14-de-identification/14-RESEARCH.md
@src/SharpDicom/Data/DicomDataset.cs
@src/SharpDicom/Data/DicomTag.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PHI detector interface and region types</name>
  <files>src/SharpDicom/Deidentification/PixelCleaner/IBurnedInPhiDetector.cs</files>
  <action>
Create interface and supporting types:
```csharp
namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// Represents a detected PHI region in pixel data.
/// </summary>
/// <param name="X">X coordinate (left edge).</param>
/// <param name="Y">Y coordinate (top edge).</param>
/// <param name="Width">Region width in pixels.</param>
/// <param name="Height">Region height in pixels.</param>
/// <param name="Confidence">Detection confidence (0.0-1.0).</param>
/// <param name="Source">Detection source (Heuristic, OCR, etc).</param>
public readonly record struct PhiRegion(
    int X,
    int Y,
    int Width,
    int Height,
    float Confidence,
    string Source);

/// <summary>
/// Result of PHI detection scan.
/// </summary>
public sealed class PhiDetectionResult
{
    public IReadOnlyList<PhiRegion> Regions { get; init; } = Array.Empty<PhiRegion>();
    public bool HasHighRiskModality { get; init; }
    public string? Modality { get; init; }
    public bool BurnedInAnnotationPresent { get; init; }
    public string? BurnedInAnnotationValue { get; init; }
}

/// <summary>
/// Interface for burned-in PHI detection in pixel data.
/// </summary>
public interface IBurnedInPhiDetector
{
    /// <summary>
    /// Detects potential PHI regions in the image.
    /// </summary>
    /// <param name="pixelData">Raw pixel data bytes.</param>
    /// <param name="width">Image width.</param>
    /// <param name="height">Image height.</param>
    /// <param name="bitsAllocated">Bits per pixel (8, 16, etc).</param>
    /// <param name="samplesPerPixel">1 for grayscale, 3 for RGB.</param>
    /// <param name="modality">DICOM modality code (US, CT, SC, etc).</param>
    /// <param name="ct">Cancellation token.</param>
    /// <returns>Detection result with regions.</returns>
    ValueTask<PhiDetectionResult> DetectAsync(
        ReadOnlyMemory<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel,
        string? modality,
        CancellationToken ct = default);
}

/// <summary>
/// High-risk modalities for burned-in PHI per research.
/// </summary>
public static class HighRiskModalities
{
    /// <summary>Ultrasound - 100% risk per research.</summary>
    public const string Ultrasound = "US";
    /// <summary>Secondary Capture - frequently contains burned-in text.</summary>
    public const string SecondaryCa = "SC";
    /// <summary>X-Ray Angiography - often has patient info overlays.</summary>
    public const string XRayAngiography = "XA";
    /// <summary>Endoscopy - high burned-in text rate.</summary>
    public const string Endoscopy = "ES";
    /// <summary>RF Fluoroscopy - patient info often visible.</summary>
    public const string RfFluoroscopy = "RF";

    public static readonly IReadOnlySet<string> All = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
    {
        Ultrasound, SecondaryCa, XRayAngiography, Endoscopy, RfFluoroscopy
    };

    public static bool IsHighRisk(string? modality)
        => modality != null && All.Contains(modality);
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>IBurnedInPhiDetector interface and PhiRegion types defined</done>
</task>

<task type="auto">
  <name>Task 2: Create BurnedInPhiRegions and HeuristicPhiDetector</name>
  <files>
src/SharpDicom/Deidentification/PixelCleaner/BurnedInPhiRegions.cs
src/SharpDicom/Deidentification/PixelCleaner/HeuristicPhiDetector.cs
  </files>
  <action>
Create modality-specific region definitions:
```csharp
namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// Predefined PHI-prone regions by modality based on common equipment patterns.
/// </summary>
public static class BurnedInPhiRegions
{
    /// <summary>
    /// Region template with relative coordinates.
    /// Negative values are from opposite edge; -1 means full dimension.
    /// </summary>
    public readonly record struct RegionTemplate(int X, int Y, int Width, int Height);

    // Ultrasound: corners and header/footer regions
    private static readonly RegionTemplate[] UltrasoundRegions = new[]
    {
        new RegionTemplate(0, 0, -1, 80),       // Top banner (full width)
        new RegionTemplate(0, -60, -1, 60),     // Bottom banner
        new RegionTemplate(0, 0, 150, 150),     // Top-left corner
        new RegionTemplate(-150, 0, 150, 150),  // Top-right corner
        new RegionTemplate(0, -100, 150, 100),  // Bottom-left corner
        new RegionTemplate(-150, -100, 150, 100) // Bottom-right corner
    };

    // CT/MR: typically minimal burned-in except corners
    private static readonly RegionTemplate[] CtMrRegions = new[]
    {
        new RegionTemplate(0, 0, 120, 100),     // Top-left corner
        new RegionTemplate(-120, 0, 120, 100),  // Top-right corner
        new RegionTemplate(0, -80, 120, 80),    // Bottom-left corner
    };

    // Secondary Capture: assume worst case (same as ultrasound)
    private static readonly RegionTemplate[] SecondaryCapture = UltrasoundRegions;

    // XA: header regions with patient/study info
    private static readonly RegionTemplate[] XaRegions = new[]
    {
        new RegionTemplate(0, 0, -1, 100),      // Top banner
        new RegionTemplate(0, -80, -1, 80),     // Bottom banner
        new RegionTemplate(0, 0, 200, 200),     // Top-left corner
    };

    /// <summary>
    /// Gets PHI-prone regions for the given modality and image dimensions.
    /// </summary>
    public static IReadOnlyList<PhiRegion> GetRegions(string? modality, int width, int height)
    {
        var templates = modality?.ToUpperInvariant() switch
        {
            "US" => UltrasoundRegions,
            "CT" or "MR" or "MG" or "DX" or "CR" => CtMrRegions,
            "SC" => SecondaryCapture,
            "XA" or "RF" => XaRegions,
            "ES" => UltrasoundRegions, // Endoscopy similar to US
            _ => Array.Empty<RegionTemplate>()
        };

        if (templates.Length == 0)
            return Array.Empty<PhiRegion>();

        var regions = new List<PhiRegion>(templates.Length);
        foreach (var t in templates)
        {
            var region = NormalizeRegion(t, width, height);
            if (region.Width > 0 && region.Height > 0)
            {
                regions.Add(region);
            }
        }
        return regions;
    }

    private static PhiRegion NormalizeRegion(RegionTemplate t, int width, int height)
    {
        // Convert relative coordinates to absolute
        var x = t.X >= 0 ? t.X : width + t.X;
        var y = t.Y >= 0 ? t.Y : height + t.Y;
        var w = t.Width == -1 ? width : (t.Width > 0 ? t.Width : width + t.Width);
        var h = t.Height == -1 ? height : (t.Height > 0 ? t.Height : height + t.Height);

        // Clamp to image bounds
        x = Math.Max(0, Math.Min(x, width - 1));
        y = Math.Max(0, Math.Min(y, height - 1));
        w = Math.Min(w, width - x);
        h = Math.Min(h, height - y);

        return new PhiRegion(x, y, w, h, 0.7f, "Heuristic");
    }
}
```

Create heuristic detector:
```csharp
namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// Detects PHI regions using modality-specific heuristics (no OCR).
/// </summary>
public sealed class HeuristicPhiDetector : IBurnedInPhiDetector
{
    /// <summary>
    /// Detects PHI regions based on modality-specific patterns.
    /// </summary>
    public ValueTask<PhiDetectionResult> DetectAsync(
        ReadOnlyMemory<byte> pixelData,
        int width,
        int height,
        int bitsAllocated,
        int samplesPerPixel,
        string? modality,
        CancellationToken ct = default)
    {
        var regions = BurnedInPhiRegions.GetRegions(modality, width, height);
        var isHighRisk = HighRiskModalities.IsHighRisk(modality);

        var result = new PhiDetectionResult
        {
            Regions = regions,
            HasHighRiskModality = isHighRisk,
            Modality = modality,
            BurnedInAnnotationPresent = false, // Caller should check tag
            BurnedInAnnotationValue = null
        };

        return ValueTask.FromResult(result);
    }
}
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>Heuristic detector returns modality-specific regions</done>
</task>

<task type="auto">
  <name>Task 3: Create OverlayPlaneProcessor for 60xx groups</name>
  <files>src/SharpDicom/Deidentification/PixelCleaner/OverlayPlaneProcessor.cs</files>
  <action>
Create overlay plane processor:
```csharp
namespace SharpDicom.Deidentification.PixelCleaner;

/// <summary>
/// Processes DICOM overlay planes (group 60xx) for PHI removal.
/// Overlay planes can contain text annotations separate from pixel data.
/// </summary>
public static class OverlayPlaneProcessor
{
    // Overlay groups: 6000, 6002, 6004, ... 601E (16 possible)
    private const ushort OverlayGroupBase = 0x6000;
    private const ushort OverlayGroupMax = 0x601E;

    // Overlay tags relative to group
    private const ushort OverlayRows = 0x0010;
    private const ushort OverlayColumns = 0x0011;
    private const ushort OverlayType = 0x0040;
    private const ushort OverlayOrigin = 0x0050;
    private const ushort OverlayBitsAllocated = 0x0100;
    private const ushort OverlayBitPosition = 0x0102;
    private const ushort OverlayData = 0x3000;
    private const ushort OverlayDescription = 0x0022;
    private const ushort OverlayLabel = 0x1500;

    /// <summary>
    /// Gets all overlay plane groups present in the dataset.
    /// </summary>
    public static IEnumerable<ushort> GetOverlayGroups(DicomDataset dataset)
    {
        for (ushort group = OverlayGroupBase; group <= OverlayGroupMax; group += 2)
        {
            var dataTag = new DicomTag(group, OverlayData);
            if (dataset.Contains(dataTag))
            {
                yield return group;
            }
        }
    }

    /// <summary>
    /// Checks if any overlay planes exist in the dataset.
    /// </summary>
    public static bool HasOverlayPlanes(DicomDataset dataset)
    {
        for (ushort group = OverlayGroupBase; group <= OverlayGroupMax; group += 2)
        {
            if (dataset.Contains(new DicomTag(group, OverlayData)))
                return true;
        }
        return false;
    }

    /// <summary>
    /// Removes all overlay planes from the dataset.
    /// </summary>
    public static void RemoveAllOverlays(DicomDataset dataset)
    {
        var tagsToRemove = new List<DicomTag>();

        foreach (var element in dataset)
        {
            var group = element.Tag.Group;
            if (group >= OverlayGroupBase && group <= OverlayGroupMax)
            {
                tagsToRemove.Add(element.Tag);
            }
        }

        foreach (var tag in tagsToRemove)
        {
            dataset.Remove(tag);
        }
    }

    /// <summary>
    /// Clears overlay data (sets to zeros) while preserving metadata.
    /// Use when you need to preserve overlay structure but remove content.
    /// </summary>
    public static void ClearOverlayData(DicomDataset dataset)
    {
        for (ushort group = OverlayGroupBase; group <= OverlayGroupMax; group += 2)
        {
            var dataTag = new DicomTag(group, OverlayData);
            var element = dataset[dataTag];

            if (element != null)
            {
                // Create zero-filled overlay data of same size
                var length = element.Length;
                var zeros = new byte[length];
                dataset.Add(DicomElement.Create(dataTag, element.VR, zeros));
            }
        }
    }

    /// <summary>
    /// Gets overlay plane metadata.
    /// </summary>
    public static OverlayPlaneInfo? GetOverlayInfo(DicomDataset dataset, ushort group)
    {
        var rowsTag = new DicomTag(group, OverlayRows);
        var colsTag = new DicomTag(group, OverlayColumns);

        if (!dataset.Contains(rowsTag) || !dataset.Contains(colsTag))
            return null;

        var rows = GetUShort(dataset, rowsTag);
        var cols = GetUShort(dataset, colsTag);
        var type = GetString(dataset, new DicomTag(group, OverlayType));
        var desc = GetString(dataset, new DicomTag(group, OverlayDescription));
        var label = GetString(dataset, new DicomTag(group, OverlayLabel));

        return new OverlayPlaneInfo(group, rows, cols, type, desc, label);
    }

    private static ushort GetUShort(DicomDataset ds, DicomTag tag)
    {
        if (ds[tag] is DicomNumericElement ne)
            return ne.GetUInt16();
        return 0;
    }

    private static string? GetString(DicomDataset ds, DicomTag tag)
    {
        if (ds[tag] is DicomStringElement se)
            return se.GetString();
        return null;
    }
}

/// <summary>
/// Overlay plane metadata.
/// </summary>
public readonly record struct OverlayPlaneInfo(
    ushort Group,
    ushort Rows,
    ushort Columns,
    string? Type,
    string? Description,
    string? Label);
```
  </action>
  <verify>
`dotnet build src/SharpDicom/SharpDicom.csproj`
  </verify>
  <done>OverlayPlaneProcessor handles 60xx group detection and removal</done>
</task>

</tasks>

<verification>
1. `dotnet build` - entire solution builds
2. `dotnet test` - existing tests pass
3. Verify high-risk modality detection: US, SC, XA, ES, RF flagged
</verification>

<success_criteria>
- IBurnedInPhiDetector interface enables detection implementations
- HeuristicPhiDetector identifies common PHI regions by modality
- BurnedInPhiRegions defines modality-specific region templates
- HighRiskModalities flags US, SC, XA, ES, RF
- OverlayPlaneProcessor detects and removes 60xx overlay planes
</success_criteria>

<output>
After completion, create `.planning/phases/14-de-identification/14-04-SUMMARY.md`
</output>
