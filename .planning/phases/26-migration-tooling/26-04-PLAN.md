---
phase: 26-migration-tooling
plan: 04
type: execute
wave: 4
depends_on: ["26-02", "26-03"]
files_modified:
  - tests/SharpDicom.Migration.Integration/NccidCompatTests.cs
  - tests/SharpDicom.Migration.Integration/NccidPatches.cs
autonomous: false

must_haves:
  truths:
    - "nccid source code compiles with SharpDicom.FoDicom5.Compat replacing fo-dicom reference"
    - "nccid C-FIND query construction works (DicomCFindRequest + Dataset.AddOrUpdate)"
    - "nccid DicomClientFactory.Create returns usable client"
    - "nccid OnResponseReceived callbacks fire with correct DicomCFindResponse data"
  artifacts:
    - path: "tests/SharpDicom.Migration.Integration/NccidCompatTests.cs"
      provides: "Integration tests proving nccid works with compat layer"
  key_links:
    - from: "tests/SharpDicom.Migration.Integration/NccidCompatTests.cs"
      to: "src/SharpDicom.FoDicom5.Compat/Network/"
      via: "compat network types replacing fo-dicom network"
      pattern: "DicomClientFactory|DicomCFindRequest"
---

<objective>
Validate that nccid compiles and works correctly against SharpDicom.FoDicom5.Compat. nccid adds networking (DicomClient, DicomCFindRequest, C-FIND queries) on top of file I/O. This is the second and final phase gate.

Purpose: Prove the network adapter layer works as a drop-in replacement for fo-dicom's networking in a real-world project. nccid exercises DicomClientFactory.Create, AddRequestAsync, SendAsync, DicomCFindRequest with OnResponseReceived, and DicomDataset.AddOrUpdate/GetSingleValue.
Output: Integration tests that build nccid source against compat layer and verify correct query behavior.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-migration-tooling/26-CONTEXT.md
@.planning/phases/26-migration-tooling/26-RESEARCH.md
@.planning/phases/26-migration-tooling/26-01-SUMMARY.md
@.planning/phases/26-migration-tooling/26-02-SUMMARY.md
@.planning/phases/26-migration-tooling/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build nccid against compat layer and create integration tests</name>
  <files>
    tests/SharpDicom.Migration.Integration/NccidCompatTests.cs
    tests/SharpDicom.Migration.Integration/NccidPatches.cs
  </files>
  <action>
    1. Check `/Users/jas88/Developer/Github/nccid/` exists. If not, clone from GitHub (jas88 fork).

    2. Examine nccid source code:
       - Read all source files to understand exact fo-dicom API usage
       - Identify all `using FellowOakDicom` and `using FellowOakDicom.Network` references
       - Note any APIs not yet in the compat layer

    3. Add nccid source files to the existing `SharpDicom.Migration.Integration` project:
       - Include as linked files or copy
       - Do NOT add fo-dicom NuGet reference

    4. Attempt to compile. For each compilation error:
       - Missing compat API: add to SharpDicom.FoDicom5.Compat
       - Namespace issue: ensure `FellowOakDicom.Network` and `FellowOakDicom.Network.Client` resolve
       - Small adaptation needed: create `NccidPatches.cs` with documented patches

    5. **Critical nccid APIs from RESEARCH.md** that must work:
       - `DicomClientFactory.Create(host, port, useTls, callingAE, calledAE)`
       - `client.NegotiateAsyncOps()` (can be no-op)
       - `client.AddRequestAsync(DicomCFindRequest)`
       - `client.SendAsync()`
       - `new DicomCFindRequest(DicomQueryRetrieveLevel.Study)`
       - `request.Dataset.AddOrUpdate(DicomTag.StudyDate, dateRange)`
       - `request.Dataset.AddOrUpdate(new DicomTag(0x8, 0x5), value)`
       - `request.OnResponseReceived += (req, resp) => { ... }`
       - `response.Dataset.GetSingleValue<string>(DicomTag.PatientID)`
       - Static tag references: `DicomTag.StudyDate`, `DicomTag.PatientID`, etc.

    6. Create integration tests `NccidCompatTests.cs`:
       - Test DicomCFindRequest construction with Study level
       - Test Dataset.AddOrUpdate with tag + string value
       - Test DicomClientFactory.Create returns usable client
       - Test AddRequestAsync buffers the request
       - Test query key population (StudyDate range, PatientID, etc.)
       - Test OnResponseReceived callback is invocable
       - For actual network tests: use SharpDicom's DicomServer as mock PACS
         - Start local DicomServer with C-FIND SCP handler
         - Create compat DicomClient, add C-FIND request, send
         - Verify OnResponseReceived fires with correct data
         - (Mark these as [Category("Integration")] if they need a running server)

    7. Iterate until compilation is clean and tests pass.
  </action>
  <verify>
    `dotnet build tests/SharpDicom.Migration.Integration/SharpDicom.Migration.Integration.csproj` compiles cleanly.
    `dotnet test tests/SharpDicom.Migration.Integration/ --filter "Nccid" --logger "console;verbosity=normal"` passes all nccid tests.
  </verify>
  <done>
    nccid source compiles against SharpDicom.FoDicom5.Compat. Network adapter translates between fo-dicom's request-queue pattern and SharpDicom's direct async pattern. C-FIND queries work end-to-end. Integration tests verify correct behavior.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>nccid compiled and running against SharpDicom.FoDicom5.Compat. Network adapter bridges fo-dicom's request-queue API to SharpDicom's direct async API. Both phase gates (dcm2csv + nccid) are now met.</what-built>
  <how-to-verify>
    1. Run `dotnet test tests/SharpDicom.Migration.Integration/ -v normal` -- all dcm2csv AND nccid tests pass
    2. Review any patches in NccidPatches.cs -- acceptable workarounds?
    3. Review the DicomClient adapter in Network/Client/DicomClient.cs -- does the request-queue translation look correct?
    4. Run `dotnet test` on the full solution -- no regressions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the nccid migration</resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build tests/SharpDicom.Migration.Integration/` -- nccid compiles against compat layer
2. `dotnet test tests/SharpDicom.Migration.Integration/` -- all integration tests pass
3. `dotnet test` -- all existing tests still pass
</verification>

<success_criteria>
- nccid source compiles with SharpDicom.FoDicom5.Compat, zero fo-dicom dependency
- DicomClientFactory.Create produces a working compat client
- DicomCFindRequest with OnResponseReceived pattern works
- C-FIND query construction via Dataset.AddOrUpdate works
- Network roundtrip test (compat client -> SharpDicom server) passes
- Both phase gates met: dcm2csv + nccid validated
</success_criteria>

<output>
After completion, create `.planning/phases/26-migration-tooling/26-04-SUMMARY.md`
</output>
