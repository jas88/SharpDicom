---
phase: 26-migration-tooling
plan: 05
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/SharpDicom.FoDicom4.Compat/SharpDicom.FoDicom4.Compat.csproj
  - src/SharpDicom.FoDicom4.Compat/DicomTag.cs
  - src/SharpDicom.FoDicom4.Compat/DicomVR.cs
  - src/SharpDicom.FoDicom4.Compat/DicomUID.cs
  - src/SharpDicom.FoDicom4.Compat/DicomItem.cs
  - src/SharpDicom.FoDicom4.Compat/DicomElement.cs
  - src/SharpDicom.FoDicom4.Compat/DicomStringElement.cs
  - src/SharpDicom.FoDicom4.Compat/DicomAttributeTag.cs
  - src/SharpDicom.FoDicom4.Compat/DicomOtherElement.cs
  - src/SharpDicom.FoDicom4.Compat/DicomSequence.cs
  - src/SharpDicom.FoDicom4.Compat/DicomDataset.cs
  - src/SharpDicom.FoDicom4.Compat/DicomFile.cs
  - src/SharpDicom.FoDicom4.Compat/DicomDictionaryEntry.cs
  - src/SharpDicom.FoDicom4.Compat/Exceptions/DicomDataException.cs
  - src/SharpDicom.FoDicom4.Compat/Compatibility.cs
  - tests/SharpDicom.FoDicom4.Compat.Tests/SharpDicom.FoDicom4.Compat.Tests.csproj
  - tests/SharpDicom.FoDicom4.Compat.Tests/FoDicom4CompatTests.cs
  - SharpDicom.sln
  - Directory.Build.props
autonomous: true

must_haves:
  truths:
    - "FoDicom4 compat uses Dicom namespace (not FellowOakDicom)"
    - "DicomDataset.Get<T>(tag) method exists (fo-dicom 4.x API, not GetSingleValue)"
    - "DicomFile.Open works identically to FoDicom5 compat"
    - "All core types mirror FoDicom5 compat with namespace and API adjustments"
  artifacts:
    - path: "src/SharpDicom.FoDicom4.Compat/DicomDataset.cs"
      provides: "FoDicom4 dataset with Get<T> API"
      contains: "namespace Dicom"
    - path: "src/SharpDicom.FoDicom4.Compat/DicomFile.cs"
      provides: "FoDicom4 file I/O"
      contains: "namespace Dicom"
    - path: "tests/SharpDicom.FoDicom4.Compat.Tests/FoDicom4CompatTests.cs"
      provides: "FoDicom4 API verification"
  key_links:
    - from: "src/SharpDicom.FoDicom4.Compat/"
      to: "SharpDicom.Data"
      via: "same composition pattern as FoDicom5"
      pattern: "SharpDicom\\.Data"
---

<objective>
Create SharpDicom.FoDicom4.Compat as a namespace-adjusted copy of FoDicom5.Compat. fo-dicom 4.x uses `Dicom` namespace (not `FellowOakDicom`) and has different API names (e.g., `Get<T>` instead of `GetSingleValue<T>`).

Purpose: Complete the "both 4.x and 5.x" requirement from CONTEXT.md. FoDicom4 compat enables migration of older projects (SmiServices, RdmpDicom in future phases).
Output: SharpDicom.FoDicom4.Compat package with `Dicom` namespace and fo-dicom 4.x API surface, plus basic tests.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-migration-tooling/26-CONTEXT.md
@.planning/phases/26-migration-tooling/26-RESEARCH.md
@.planning/phases/26-migration-tooling/26-01-SUMMARY.md

# FoDicom5 compat as template
@src/SharpDicom.FoDicom5.Compat/DicomDataset.cs
@src/SharpDicom.FoDicom5.Compat/DicomFile.cs
@src/SharpDicom.FoDicom5.Compat/DicomTag.cs
@src/SharpDicom.FoDicom5.Compat/DicomItem.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FoDicom4.Compat project with Dicom namespace and 4.x API</name>
  <files>
    src/SharpDicom.FoDicom4.Compat/SharpDicom.FoDicom4.Compat.csproj
    src/SharpDicom.FoDicom4.Compat/DicomTag.cs
    src/SharpDicom.FoDicom4.Compat/DicomVR.cs
    src/SharpDicom.FoDicom4.Compat/DicomUID.cs
    src/SharpDicom.FoDicom4.Compat/DicomItem.cs
    src/SharpDicom.FoDicom4.Compat/DicomElement.cs
    src/SharpDicom.FoDicom4.Compat/DicomStringElement.cs
    src/SharpDicom.FoDicom4.Compat/DicomAttributeTag.cs
    src/SharpDicom.FoDicom4.Compat/DicomOtherElement.cs
    src/SharpDicom.FoDicom4.Compat/DicomSequence.cs
    src/SharpDicom.FoDicom4.Compat/DicomDataset.cs
    src/SharpDicom.FoDicom4.Compat/DicomFile.cs
    src/SharpDicom.FoDicom4.Compat/DicomDictionaryEntry.cs
    src/SharpDicom.FoDicom4.Compat/Exceptions/DicomDataException.cs
    src/SharpDicom.FoDicom4.Compat/Compatibility.cs
    SharpDicom.sln
    Directory.Build.props
  </files>
  <action>
    1. Create `src/SharpDicom.FoDicom4.Compat/SharpDicom.FoDicom4.Compat.csproj`:
       - Same structure as FoDicom5.Compat.csproj
       - PackageId: SharpDicom.FoDicom4.Compat
       - Reference SharpDicom project

    2. Add project TFM configuration to `Directory.Build.props` and add to solution

    3. Copy ALL source files from `src/SharpDicom.FoDicom5.Compat/` to `src/SharpDicom.FoDicom4.Compat/`

    4. Apply the following transformations to ALL files:
       - Replace namespace `FellowOakDicom` with `Dicom` (all occurrences)
       - Replace namespace `FellowOakDicom.Network` with `Dicom.Network`
       - Replace namespace `FellowOakDicom.Network.Client` with `Dicom.Network.Client`

    5. Apply API differences from RESEARCH.md fo-dicom 4.x vs 5.x table:

       **DicomDataset.cs**:
       - Add `Get<T>(DicomTag tag, int index = 0)` method (fo-dicom 4.x primary API)
       - Keep `GetSingleValue<T>` as it also exists in late 4.x, but `Get<T>` is primary
       - Add `Get<T>(DicomTag tag, T defaultValue)` overload

       **Network/Client/DicomClient.cs** (if present):
       - Constructor pattern: `new DicomClient(host, port, useTls, callingAE, calledAE)` instead of factory
       - fo-dicom 4.x uses direct constructor, not DicomClientFactory

       No network types needed for FoDicom4 in this plan (no 4.x validation target yet). Include network stubs only if they were copied from FoDicom5. If complex, omit network subdirectory.

    6. Build and fix any compilation issues.
  </action>
  <verify>
    `dotnet build src/SharpDicom.FoDicom4.Compat/SharpDicom.FoDicom4.Compat.csproj` succeeds with zero errors.
  </verify>
  <done>
    SharpDicom.FoDicom4.Compat project exists with `Dicom` namespace, `Get<T>` API on DicomDataset, and all core types mirroring FoDicom5 with namespace/API adjustments. Project compiles across all targets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FoDicom4 compat tests</name>
  <files>
    tests/SharpDicom.FoDicom4.Compat.Tests/SharpDicom.FoDicom4.Compat.Tests.csproj
    tests/SharpDicom.FoDicom4.Compat.Tests/FoDicom4CompatTests.cs
    Directory.Build.props
  </files>
  <action>
    1. Create test project `tests/SharpDicom.FoDicom4.Compat.Tests/`:
       - Target `net10.0`
       - Reference SharpDicom.FoDicom4.Compat project
       - Add to solution

    2. Add project TFM configuration to `Directory.Build.props`

    3. Create `FoDicom4CompatTests.cs` verifying:
       - `using Dicom;` compiles (not FellowOakDicom)
       - `DicomFile.Open(path)` works
       - `dataset.Get<string>(tag)` returns correct value (4.x API)
       - `dataset.Get<string>(tag, "default")` returns default for missing tag
       - `dataset.GetSingleValue<string>(tag)` also works (late 4.x)
       - DicomTag.DictionaryEntry.Name resolves
       - Dataset enumeration yields correct DicomItem subtypes
       - AddOrUpdate works

    4. Run tests
  </action>
  <verify>
    `dotnet test tests/SharpDicom.FoDicom4.Compat.Tests/ --logger "console;verbosity=normal"` passes all tests.
    `dotnet test` on the full solution passes.
  </verify>
  <done>
    FoDicom4 compat verified with tests using `Dicom` namespace and `Get<T>` API. All core functionality mirrors FoDicom5 compat with correct namespace and API adjustments.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SharpDicom.FoDicom4.Compat/` -- project builds
2. `dotnet test tests/SharpDicom.FoDicom4.Compat.Tests/` -- tests pass
3. `dotnet test` -- all tests pass, no regressions
4. Verify `using Dicom;` namespace is used, not `FellowOakDicom`
</verification>

<success_criteria>
- SharpDicom.FoDicom4.Compat compiles with `Dicom` namespace
- `Get<T>` method exists and works (fo-dicom 4.x primary API)
- All core types present (DicomFile, DicomDataset, DicomTag, DicomItem, etc.)
- Tests verify namespace correctness and 4.x API surface
- Full solution builds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-migration-tooling/26-05-SUMMARY.md`
</output>
