---
phase: 26-migration-tooling
plan: 03
type: execute
wave: 3
depends_on: ["26-01"]
files_modified:
  - tests/SharpDicom.Migration.Integration/SharpDicom.Migration.Integration.csproj
  - tests/SharpDicom.Migration.Integration/Dcm2CsvCompatTests.cs
  - tests/SharpDicom.Migration.Integration/Dcm2CsvPatches.cs
  - SharpDicom.sln
  - Directory.Build.props
autonomous: false

must_haves:
  truths:
    - "dcm2csv source code compiles with SharpDicom.FoDicom5.Compat replacing fo-dicom reference"
    - "dcm2csv DICOM file processing produces correct CSV output"
    - "dcm2csv pattern matching on DicomItem subtypes works (DicomStringElement, DicomSequence, DicomAttributeTag)"
  artifacts:
    - path: "tests/SharpDicom.Migration.Integration/Dcm2CsvCompatTests.cs"
      provides: "Integration tests proving dcm2csv works with compat layer"
    - path: "tests/SharpDicom.Migration.Integration/SharpDicom.Migration.Integration.csproj"
      provides: "Integration test project referencing dcm2csv source and compat layer"
  key_links:
    - from: "tests/SharpDicom.Migration.Integration/Dcm2CsvCompatTests.cs"
      to: "src/SharpDicom.FoDicom5.Compat/"
      via: "project reference replaces fo-dicom NuGet"
      pattern: "FellowOakDicom"
---

<objective>
Validate that dcm2csv compiles and works correctly against SharpDicom.FoDicom5.Compat. dcm2csv is a file-I/O-only tool (no networking) that reads DICOM files and outputs CSV. This is the first phase gate per CONTEXT.md.

Purpose: Prove the compat layer actually works as a drop-in replacement for fo-dicom in a real-world project. dcm2csv exercises DicomFile.Open, dataset enumeration, DicomStringElement.Get, DicomSequence.Items, DicomAttributeTag.Values, and DicomTag.DictionaryEntry.Name.
Output: Integration test project that builds dcm2csv source against compat layer and verifies correct output.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-migration-tooling/26-CONTEXT.md
@.planning/phases/26-migration-tooling/26-RESEARCH.md
@.planning/phases/26-migration-tooling/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dcm2csv against compat layer and fix compilation issues</name>
  <files>
    tests/SharpDicom.Migration.Integration/SharpDicom.Migration.Integration.csproj
    tests/SharpDicom.Migration.Integration/Dcm2CsvPatches.cs
    SharpDicom.sln
    Directory.Build.props
  </files>
  <action>
    1. Check if dcm2csv source directory exists at `/Users/jas88/Developer/Github/dcm2csv/`. If not, clone from GitHub (jas88 fork): `git clone https://github.com/jas88/dcm2csv.git /Users/jas88/Developer/Github/dcm2csv/`

    2. Verify the dcm2csv source directory is populated: confirm key source files exist (e.g., the main `.cs` file(s) with `using FellowOakDicom`). If the clone failed or directory is empty, fail early with a clear error.

    3. Examine dcm2csv source code:
       - Read the main source files to understand exact fo-dicom API usage
       - Identify all `using FellowOakDicom` references and all fo-dicom types used
       - Note any APIs not yet in the compat layer from Plan 01

    4. Create integration test project `tests/SharpDicom.Migration.Integration/`:
       - Target `net10.0`
       - Reference `SharpDicom.FoDicom5.Compat` project
       - Reference NUnit, NUnit3TestAdapter, Microsoft.NET.Test.Sdk
       - Include dcm2csv source files as linked files (`<Compile Include="..." Link="..." />`) or copy them
       - Do NOT reference fo-dicom NuGet -- the entire point is to compile against compat layer

    5. Add project TFM configuration to `Directory.Build.props`
    6. Add to solution via `dotnet sln add`

    7. Attempt to compile. For each compilation error:
       - If it's a missing API in the compat layer: add the missing API to SharpDicom.FoDicom5.Compat (update Plan 01 files as needed)
       - If it's a namespace issue: the dcm2csv source uses `using FellowOakDicom;` which should resolve to the compat namespace
       - If it requires a small adaptation in dcm2csv: create `Dcm2CsvPatches.cs` with partial classes or extension methods to bridge gaps. Document each patch with comments explaining why.

    8. **Critical dcm2csv APIs from RESEARCH.md** that must work:
       - `DicomFile.Open(string)` -> compat DicomFile
       - `file.Dataset` enumeration (`IEnumerable<DicomItem>`)
       - Pattern matching: `item is DicomStringElement`, `item is DicomSequence`, `item is DicomAttributeTag`
       - `DicomStringElement.Count` and `.Get<string>(index)`
       - `DicomSequence.Items`
       - `DicomAttributeTag.Values` (returns `DicomTag[]`)
       - `DicomTag.DictionaryEntry.Name`
       - `DicomItem.Tag`
       - `DicomItem.ToString()`

    9. Iterate until `dotnet build tests/SharpDicom.Migration.Integration/` compiles cleanly.
  </action>
  <verify>
    1. Verify dcm2csv source directory exists: `ls /Users/jas88/Developer/Github/dcm2csv/*.cs` or equivalent confirms source files are present.
    2. `dotnet build tests/SharpDicom.Migration.Integration/SharpDicom.Migration.Integration.csproj` succeeds with zero errors.
  </verify>
  <done>
    dcm2csv source code compiles against SharpDicom.FoDicom5.Compat with no fo-dicom dependency. Any required patches are documented in Dcm2CsvPatches.cs. All missing compat APIs have been added to the compat project.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dcm2csv integration tests and verify correct output</name>
  <files>
    tests/SharpDicom.Migration.Integration/Dcm2CsvCompatTests.cs
  </files>
  <action>
    1. Create integration tests that exercise dcm2csv's core functionality:
       - Create a test DICOM file (or use existing test fixtures from SharpDicom.Tests)
       - Call dcm2csv's tag processing logic (the `Entry.ProcessTag` method or equivalent)
       - Verify CSV output matches expected format

    2. Test cases:
       - Process a DICOM file with standard string elements -> correct CSV rows
       - Process a DICOM file with sequences -> sequence items appear in output
       - Process a DICOM file with AT (Attribute Tag) elements -> tag values formatted correctly
       - Process a DICOM file with multiple VM values -> all values captured
       - Process empty/missing elements -> handled gracefully (no crash)

    3. If dcm2csv has its own test suite:
       - Attempt to include and run those tests against the compat layer
       - Fix any test failures by adding missing compat APIs

    4. Run all tests: `dotnet test tests/SharpDicom.Migration.Integration/`
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Migration.Integration/ --logger "console;verbosity=normal"` passes all tests.
    `dotnet test` on the entire solution passes (no regressions).
  </verify>
  <done>
    dcm2csv processes DICOM files correctly through the compat layer. Integration tests verify CSV output. Pattern matching on DicomItem subtypes works. This satisfies the first phase gate (dcm2csv compiles and passes tests).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>dcm2csv compiled and running against SharpDicom.FoDicom5.Compat instead of fo-dicom. Integration tests verify correct DICOM file processing.</what-built>
  <how-to-verify>
    1. Run `dotnet test tests/SharpDicom.Migration.Integration/ -v normal` and confirm all tests pass
    2. If you have dcm2csv installed, try running it against a real DICOM file to verify output format
    3. Review any patches in Dcm2CsvPatches.cs -- are they acceptable workarounds?
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the dcm2csv migration</resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build tests/SharpDicom.Migration.Integration/` -- dcm2csv compiles against compat layer
2. `dotnet test tests/SharpDicom.Migration.Integration/` -- integration tests pass
3. `dotnet test` -- all existing tests still pass
</verification>

<success_criteria>
- dcm2csv source compiles with SharpDicom.FoDicom5.Compat, zero fo-dicom dependency
- DicomFile.Open processes real DICOM files correctly
- Dataset enumeration and pattern matching on DicomItem subtypes works
- DicomStringElement.Get, DicomSequence.Items, DicomAttributeTag.Values all functional
- Integration tests verify correct CSV-like output
- User approves migration quality
</success_criteria>

<output>
After completion, create `.planning/phases/26-migration-tooling/26-03-SUMMARY.md`
</output>
