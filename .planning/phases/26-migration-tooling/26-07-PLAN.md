---
phase: 26-migration-tooling
plan: 07
type: execute
wave: 4
depends_on: ["26-06"]
files_modified:
  - tests/SharpDicom.Analyzers.Tests/SharpDicom.Analyzers.Tests.csproj
  - tests/SharpDicom.Analyzers.Tests/FoDicomUsageAnalyzerTests.cs
  - tests/SharpDicom.Analyzers.Tests/CompatUsageAnalyzerTests.cs
  - tests/SharpDicom.Analyzers.Tests/FoDicomToCompatFixTests.cs
  - tests/SharpDicom.Analyzers.Tests/CompatToNativeFixTests.cs
  - SharpDicom.sln
  - Directory.Build.props
  - Directory.Packages.props
autonomous: true

must_haves:
  truths:
    - "SD0001 fires on using FellowOakDicom in test code"
    - "SD0001 fires on using Dicom (fo-dicom 4.x) in test code"
    - "SD0010 fires on using compat layer namespace in test code"
    - "FoDicomToCompatFix correctly rewrites using directives"
    - "CompatToNativeFix correctly rewrites compat usings to SharpDicom"
    - "No false positives on unrelated Dicom-named user types"
  artifacts:
    - path: "tests/SharpDicom.Analyzers.Tests/FoDicomUsageAnalyzerTests.cs"
      provides: "Analyzer diagnostic verification"
      contains: "CSharpAnalyzerTest"
    - path: "tests/SharpDicom.Analyzers.Tests/FoDicomToCompatFixTests.cs"
      provides: "Code fix verification"
      contains: "CSharpCodeFixTest"
  key_links:
    - from: "tests/SharpDicom.Analyzers.Tests/"
      to: "src/SharpDicom.Analyzers/"
      via: "Microsoft.CodeAnalysis.Testing infrastructure"
      pattern: "CSharpAnalyzerTest|CSharpCodeFixTest"
---

<objective>
Create comprehensive tests for the Roslyn analyzer and code fix providers using Microsoft.CodeAnalysis.Testing infrastructure.

Purpose: Verify that analyzers correctly detect fo-dicom patterns without false positives, and that code fix providers produce correct rewrites. Analyzer bugs would give users wrong migration advice.
Output: Test suite covering all diagnostic IDs (SD0001-SD0011) and code fix rewrites.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-migration-tooling/26-CONTEXT.md
@.planning/phases/26-migration-tooling/26-RESEARCH.md
@.planning/phases/26-migration-tooling/26-06-SUMMARY.md

@src/SharpDicom.Analyzers/DiagnosticIds.cs
@src/SharpDicom.Analyzers/Analyzers/FoDicomUsageAnalyzer.cs
@src/SharpDicom.Analyzers/CodeFixes/FoDicomToCompatFix.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analyzer test project and diagnostic tests</name>
  <files>
    tests/SharpDicom.Analyzers.Tests/SharpDicom.Analyzers.Tests.csproj
    tests/SharpDicom.Analyzers.Tests/FoDicomUsageAnalyzerTests.cs
    tests/SharpDicom.Analyzers.Tests/CompatUsageAnalyzerTests.cs
    SharpDicom.sln
    Directory.Build.props
    Directory.Packages.props
  </files>
  <action>
    1. Add test infrastructure packages to `Directory.Packages.props`:
       - `Microsoft.CodeAnalysis.CSharp.Analyzer.Testing` version 1.1.2
       - `Microsoft.CodeAnalysis.CSharp.CodeFix.Testing` version 1.1.2
       - `Microsoft.CodeAnalysis.CSharp.CodeFix.Testing.NUnit` version 1.1.2
       CRITICAL per RESEARCH.md Pitfall 7: Pin ALL Microsoft.CodeAnalysis.* packages to same version (5.0.0) to avoid version mismatch.

    2. Create `tests/SharpDicom.Analyzers.Tests/SharpDicom.Analyzers.Tests.csproj`:
       - Target `net10.0`
       - Reference SharpDicom.Analyzers project
       - Reference testing packages: Microsoft.CodeAnalysis.CSharp.Analyzer.Testing, Microsoft.CodeAnalysis.CSharp.CodeFix.Testing, Microsoft.CodeAnalysis.CSharp.CodeFix.Testing.NUnit
       - Reference NUnit, NUnit3TestAdapter, Microsoft.NET.Test.Sdk

    3. Add to solution and Directory.Build.props

    4. **FoDicomUsageAnalyzerTests.cs** using `CSharpAnalyzerTest` pattern from RESEARCH.md:

       Test cases:
       - `DetectsFoDicom5UsingDirective`: `using FellowOakDicom;` -> SD0001 reported
       - `DetectsFoDicom5NetworkUsing`: `using FellowOakDicom.Network;` -> SD0001 reported
       - `DetectsFoDicom5ClientUsing`: `using FellowOakDicom.Network.Client;` -> SD0001 reported
       - `DetectsFoDicom4UsingDirective`: `using Dicom;` -> SD0001 reported (with semantic check to avoid false positive on user namespace)
       - `IgnoresUnrelatedNamespace`: `using System.Diagnostics;` -> no diagnostic
       - `IgnoresSharpDicomNamespace`: `using SharpDicom.Data;` -> no diagnostic
       - `IgnoresUserDicomNamespace`: `namespace MyProject.Dicom { class X {} }` then `using MyProject.Dicom;` -> no diagnostic (semantic check prevents false positive)

       Each test uses the `CSharpAnalyzerTest<FoDicomUsageAnalyzer, NUnitVerifier>` pattern with `TestCode` containing source and `{|SD0001:...|}}` expected diagnostic markers.

    5. **CompatUsageAnalyzerTests.cs**:
       - `DetectsCompat5Using`: `using SharpDicom.FoDicom5.Compat.FellowOakDicom;` -> SD0010
       - `DetectsCompat4Using`: `using SharpDicom.FoDicom4.Compat.Dicom;` -> SD0010
       - `IgnoresNativeSharpDicom`: `using SharpDicom.Data;` -> no diagnostic

    6. Run: `dotnet test tests/SharpDicom.Analyzers.Tests/`
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Analyzers.Tests/ --filter "Analyzer" --logger "console;verbosity=normal"` passes all analyzer tests.
  </verify>
  <done>
    FoDicomUsageAnalyzer correctly detects fo-dicom 4.x and 5.x using directives with zero false positives on unrelated namespaces. CompatUsageAnalyzer detects compat layer usage for step-2 migration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create code fix provider tests</name>
  <files>
    tests/SharpDicom.Analyzers.Tests/FoDicomToCompatFixTests.cs
    tests/SharpDicom.Analyzers.Tests/CompatToNativeFixTests.cs
  </files>
  <action>
    1. **FoDicomToCompatFixTests.cs** using `CSharpCodeFixTest` pattern:

       Test cases:
       - `FixesFoDicom5Using`:
         TestCode: `using {|SD0001:FellowOakDicom|};`
         FixedCode: `using SharpDicom.FoDicom5.Compat.FellowOakDicom;`

       - `FixesFoDicom5NetworkUsing`:
         TestCode: `using {|SD0001:FellowOakDicom.Network|};`
         FixedCode: `using SharpDicom.FoDicom5.Compat.FellowOakDicom.Network;`

       - `FixesFoDicom5ClientUsing`:
         TestCode: `using {|SD0001:FellowOakDicom.Network.Client|};`
         FixedCode: `using SharpDicom.FoDicom5.Compat.FellowOakDicom.Network.Client;`

       - `FixesFoDicom4Using`:
         TestCode: `using {|SD0001:Dicom|};`
         FixedCode: `using SharpDicom.FoDicom4.Compat.Dicom;`

       Each test uses `CSharpCodeFixTest<FoDicomUsageAnalyzer, FoDicomToCompatFix, NUnitVerifier>`.

    2. **CompatToNativeFixTests.cs**:
       - `FixesCompat5ToNative`:
         TestCode: `using {|SD0010:SharpDicom.FoDicom5.Compat.FellowOakDicom|};`
         FixedCode: `using SharpDicom.Data;`

       - `FixesCompat5NetworkToNative`:
         TestCode: `using {|SD0010:SharpDicom.FoDicom5.Compat.FellowOakDicom.Network|};`
         FixedCode: `using SharpDicom.Network;`

    3. Run all analyzer tests: `dotnet test tests/SharpDicom.Analyzers.Tests/`
    4. Run full solution tests: `dotnet test`
  </action>
  <verify>
    `dotnet test tests/SharpDicom.Analyzers.Tests/ --logger "console;verbosity=normal"` passes all tests.
    `dotnet test` on the full solution passes (no regressions).
  </verify>
  <done>
    Code fix providers verified: FoDicomToCompatFix correctly rewrites fo-dicom using directives to compat namespaces. CompatToNativeFix correctly rewrites compat usings to native SharpDicom. All analyzer and code fix tests pass. Full solution builds and all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/SharpDicom.Analyzers.Tests/` -- all analyzer and code fix tests pass
2. `dotnet test` -- all solution tests pass (no regressions from any plan)
3. Verify zero false positives on non-fo-dicom namespaces
</verification>

<success_criteria>
- SD0001 fires on `using FellowOakDicom` and `using Dicom` but NOT on unrelated namespaces
- SD0010 fires on compat layer usings
- FoDicomToCompatFix produces correct namespace rewrites for both 4.x and 5.x
- CompatToNativeFix produces correct native SharpDicom usings
- All tests pass, zero false positives verified
- Full solution builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-migration-tooling/26-07-SUMMARY.md`
</output>
